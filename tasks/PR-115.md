# PR-115: 100% coverage for src/claude_cli.py

Branch: pr-115-coverage-claude-cli
- Type: refactor
- Complexity: medium
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/claude_cli.py` is at 81% line coverage with 20 missing lines. Module wraps Claude Code CLI invocation (sync and async).

Missing lines: `151`, `162-167`, `189-190`, `195-202`, `206`, `221`, `242`.

## Scope

Bring `src/claude_cli.py` to 100% line coverage by extending `tests/test_claude_cli.py`.

## Missing blocks

- Lines 162-167 (6 lines): error path or specific output-parsing branch
- Lines 195-202 (8 lines): larger block, likely error classification
- Scattered: singletons in exception branches and fallbacks

## Triage

All likely test-addable via subprocess mocking.

## Testing approach

Existing `tests/test_claude_cli.py` uses subprocess mocking patterns. Extend with:
- `PermissionError`, `FileNotFoundError`, `TimeoutExpired` exceptions for each CLI wrapper
- Stderr patterns that trigger rate-limit classification
- Empty or malformed stdout handling

## Tests to add

Per-line targeted tests. Include async variants (`diagnose_error_async`, `run_claude_async`) if the missing lines are in async code.

## Restrictions

Standard: no source modifications except dead/pragma. Mock `subprocess.run` and `asyncio.create_subprocess_exec` — no real subprocess execution.

## Success criteria

- `pytest tests/test_claude_cli.py --cov=src/claude_cli --cov-report=term-missing --cov-fail-under=100` exits 0
- 100% coverage for `src/claude_cli.py`
- All existing tests pass
- `ruff check src tests` exits 0
- `scripts/ci.sh` exits 0
