# PR-116: 100% coverage for src/codex_cli.py

Branch: pr-116-coverage-codex-cli
- Type: refactor
- Complexity: low
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/codex_cli.py` is at 78% line coverage with 10 missing lines. Module wraps Codex CLI invocation.

Missing lines: `59-60`, `65-72`.

Two contiguous blocks totaling 10 lines — likely one exception handler plus one alternate-path block.

## Scope

Bring `src/codex_cli.py` to 100% line coverage by extending `tests/test_codex_cli.py`.

## Missing blocks

- Lines 59-60 (2 lines): paired, likely exception handler entry
- Lines 65-72 (8 lines): larger block, likely error-classification or retry logic

## Triage

Small scope. Both blocks likely test-addable via subprocess mocking.

## Testing approach

Same pattern as `tests/test_claude_cli.py`. Mock `subprocess.run` / async equivalent with specific exit codes and stderr to trigger the missing branches.

## Tests to add

- `test_codex_<scenario_for_block_59_60>`
- `test_codex_<scenario_for_block_65_72>`

Specific names after reading source.

## Restrictions

Standard: no source modifications except dead/pragma. No real CLI.

## Success criteria

- `pytest tests/test_codex_cli.py --cov=src/codex_cli --cov-report=term-missing --cov-fail-under=100` exits 0
- 100% coverage for `src/codex_cli.py`
- All existing tests pass
- `ruff check src tests` exits 0
- `scripts/ci.sh` exits 0
