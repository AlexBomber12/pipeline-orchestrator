# PR-118: 100% coverage for src/dag.py

Branch: pr-118-coverage-dag
- Type: refactor
- Complexity: low
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/dag.py` is at 92% line coverage with 5 missing lines: `23`, `30`, `36`, `71`, `108`. Module computes task DAG and eligibility ordering (PR-086).

## Scope

Bring `src/dag.py` to 100% line coverage by extending `tests/test_dag.py`.

## Missing lines

- Lines 23, 30, 36: scattered early branches, likely cycle detection edge cases or validation guards
- Line 71: mid-function branch
- Line 108: late branch, likely fallback or default return

## Triage

Small scope. All likely test-addable with crafted task sets.

## Testing approach

Construct `list[TaskHeader]` inputs that trigger each branch:
- Empty input
- Single task with no deps
- Task with non-existent dependency
- Cyclic dependency
- Long chain with specific ordering

## Tests to add

- `test_<scenario>_for_line_23`
- `test_<scenario>_for_line_30`
- ... etc

Read source and name per actual branch.

## Restrictions

Standard: no source modifications except dead/pragma.

## Success criteria

- `pytest tests/test_dag.py --cov=src/dag --cov-report=term-missing --cov-fail-under=100` exits 0
- 100% coverage for `src/dag.py`
- All existing tests pass
- `ruff check src tests` exits 0
- `scripts/ci.sh` exits 0
