# PR-108b: 100% coverage for fix.handle_fix

Branch: pr-108b-coverage-fix-handle
- Type: refactor
- Complexity: high
- Depends on: PR-108a
- Priority: 3
- Coder: any

## Problem

Second and final sub-PR for `src/daemon/handlers/fix.py`. Covers `handle_fix()` at lines 75-321.

Missing lines in scope: `135-140`, `188-215`, `217`, `228-255`, `275-278`, `308`. Total ~35 missing lines.

After this PR lands, `src/daemon/handlers/fix.py` reaches 100% coverage.

## Scope

Bring `handle_fix()` to covered. This is the FIX state transition that runs coder on review feedback, verifies push, transitions to WATCH.

Add tests in `tests/test_runner.py`.

## Missing blocks

- Lines 135-140: 6-line block, intermediate check during fix iteration
- Lines 188-215: 28-line LARGE block, retry/rollback logic after fix attempt
- Line 217: continuation single branch
- Lines 228-255: 28-line LARGE block, alternative retry or verification path
- Lines 275-278: 4-line cleanup branch
- Line 308: late state transition

## Triage

Both large blocks are real retry/recovery paths. Must be tested.

Key invariants to preserve (from prior PRs):
- Head SHA verification after push (PR-050)
- Refresh PR branch from origin before FIX (PR-049)
- fix_iterations counter increments exactly once per successful push

## Testing approach

Mock:
- `coder_registry.get_coder(name).fix_review(...)` with controlled outcomes (success, error, timeout, rate-limited)
- `git_ops._git` for push result and head-SHA verification
- `github_client.get_pr_status` for review-state polling
- `self._current_run_record` for metrics writes and counter verification

Test state variations:
- First fix attempt (fix_iterations=0)
- Subsequent fix attempt (fix_iterations=N)
- Max iterations reached
- Coder returns error
- Push rejected due to stale base
- Push succeeds but head SHA matches (no actual change)

## Tests to add in `tests/test_runner.py`

Indicative:
- `test_handle_fix_intermediate_check_135_140`
- `test_handle_fix_retry_path_188_215_first_iteration`
- `test_handle_fix_retry_path_188_215_max_iterations`
- `test_handle_fix_alternative_path_228_255_success`
- `test_handle_fix_alternative_path_228_255_failure`
- `test_handle_fix_cleanup_275_278`
- `test_handle_fix_state_transition_308`
- `test_handle_fix_fix_iterations_counter_increments_correctly`
- `test_handle_fix_does_not_double_count_on_failed_push`

## Success criteria

- `pytest tests/test_runner.py --cov=src/daemon/handlers/fix --cov-report=term-missing --cov-fail-under=100` exits 0
- **Overall coverage for `src/daemon/handlers/fix.py` is 100%** (last in chain, final gate)
- All lines in PR-108a scope remain covered
- All existing tests pass
- `ruff check src tests` exits 0
- `scripts/ci.sh` exits 0
- No diff in `src/daemon/handlers/fix.py` except justified deletions/pragmas

## Restrictions

- Do NOT touch lines 20-74 (handled by 108a)
- Do NOT modify any other source module
- Do NOT change retry counts, timeouts, or fix semantics
- Do NOT bypass head SHA verification (PR-050 invariant)
- Do NOT bypass refresh before FIX (PR-049 invariant)
- This is the CLOSING sub-PR for fix.py — MUST achieve 100%
