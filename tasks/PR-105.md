# PR-105: 100% coverage for src/daemon/rate_limit.py

Branch: pr-105-coverage-rate-limit
- Type: refactor
- Complexity: low
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/daemon/rate_limit.py` is at 93% line coverage with 13 missing lines: `151`, `170`, `180`, `204-210`, `306`, `311-312`. Module implements three-layer rate-limit tracking (proactive usage API, in-flight statusline monitor, reactive stderr pattern).

## Scope

Bring `src/daemon/rate_limit.py` to 100% line coverage by adding targeted tests in `tests/test_rate_limit.py` (create if needed).

## Missing lines at time of task creation

Investigate source at each line:
- Lines 151, 170, 180: scattered single-line branches, likely early-returns for edge cases in rate-limit window calculation
- Lines 204-210: 7-line block, likely parsing or recovery logic for a specific rate-limit detection path
- Line 306: late branch, likely in rate_limited_coder_until update
- Lines 311-312: 2-line block, possibly the expiry-check return path for per-coder tracking

## Triage

All likely test-addable. Rate-limit paths are real runtime states. If any branch is truly unreachable given the three-layer architecture, delete or pragma with comment.

## Tests to add in `tests/test_rate_limit.py`

Specific test names depend on actual source content. Examples:
- `test_rate_limit_state_ignores_window_in_the_past`
- `test_rate_limit_apply_reactive_pattern_skips_<edge_case>`
- `test_rate_limit_per_coder_expiry_returns_<value>_when_<condition>`
- `test_rate_limit_parse_handles_<specific_format>`
- `test_rate_limit_proactive_check_fallback_when_usage_api_unavailable`

## Restrictions

- Do NOT modify `src/daemon/rate_limit.py`.
- Do NOT modify any other source module.
- Do NOT modify rate-limit thresholds or window logic (coverage task, not tuning).
- Use fake datetime / frozen time for window calculations if needed (`freezegun` or monkeypatch `datetime.now`).

## Success criteria

- `pytest tests/test_rate_limit.py --cov=src/daemon/rate_limit --cov-report=term-missing --cov-fail-under=100` exits 0.
- Overall coverage for `src/daemon/rate_limit.py` is 100%.
- All existing tests still pass.
- `ruff check src tests` exits 0.
- `scripts/ci.sh` exits 0.
- No diff in `src/daemon/rate_limit.py`.
