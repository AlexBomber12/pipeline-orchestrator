# PR-109a: 100% coverage for runner __init__ and setup helpers

Branch: pr-109a-coverage-runner-init
- Type: refactor
- Complexity: medium
- Depends on: none
- Priority: 3
- Coder: any

## Problem

First of three sub-PRs for `src/daemon/runner.py` (77% → 100%). Covers `__init__`, `app_config` setter, `set_usage_providers`, `_select_coder`, `_get_coder`, `_refresh_auth_status_cache`, `_load_current_task_metadata`, `_start_current_run_record` (lines 114-333).

Missing lines in scope: `137-157`, `159-183`, `216`, `224-225`, `268`, `275-276`, `312-313`. Total ~30 missing lines.

## Scope

Bring initialization and setup helpers to covered. Do NOT touch diff-stats/state-mgmt (PR-109b) or run_cycle/log (PR-109c).

Add tests in `tests/test_runner.py` (already the primary runner test file, extend it).

## Missing blocks

- Lines 137-157: 21-line block in `__init__`, likely conditional setup based on config (coder enable/disable, auth path selection, rate-limit provider init)
- Lines 159-183: 25-line block in `__init__`, paired or alternative setup path
- Line 216: branch in `app_config` setter
- Lines 224-225: paired branch in `set_usage_providers`
- Line 268: branch in `_refresh_auth_status_cache`
- Lines 275-276: paired in `_refresh_auth_status_cache`
- Lines 312-313: paired in `_start_current_run_record`

## Triage

Initialization paths are typically config-driven branches. Test by constructing `Runner` with various `AppConfig` and `RepoConfig` combinations.

`_refresh_auth_status_cache` branches likely test auth-status dict updates for different provider states.

## Testing approach

Use existing runner fixtures in `tests/test_runner.py`. Extend with:
- Runner construction with various config permutations (different coder sets, auth paths)
- `set_usage_providers` called with different provider combinations
- `_refresh_auth_status_cache` with mocked auth-status returns (ok, degraded, failed)
- `_start_current_run_record` with metadata variations

## Tests to add in `tests/test_runner.py`

- 1-3 tests for the 137-157 block covering `__init__` config branches
- 1-3 tests for the 159-183 block
- `test_app_config_setter_at_216_branch`
- `test_set_usage_providers_branch_224_225`
- `test_refresh_auth_status_cache_branch_268`
- `test_refresh_auth_status_cache_branch_275_276`
- `test_start_current_run_record_branch_312_313`

## Success criteria

- `pytest tests/test_runner.py --cov=src/daemon/runner --cov-report=term-missing` runs with exit 0
- Lines 137-157, 159-183, 216, 224-225, 268, 275-276, 312-313 no longer in "Missing" for runner.py
- Lines 334+ MAY still appear (scope of PR-109b and PR-109c)
- All existing runner tests pass (890+)
- `ruff check src tests` exits 0
- `scripts/ci.sh` exits 0

## Restrictions

- Do NOT touch lines 334+ in runner.py (other sub-PRs)
- Do NOT modify any other source module
- Do NOT refactor `__init__` or helpers
- Do NOT add new features
