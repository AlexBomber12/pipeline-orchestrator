# PR-114: 100% coverage for src/usage.py

Branch: pr-114-coverage-usage
- Type: refactor
- Complexity: medium
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/usage.py` is at 89% line coverage with 20 missing lines. Module implements usage API providers (OAuth for Anthropic, OpenAI for Codex) with caching.

Missing lines: `77`, `138-139`, `150`, `189`, `200`, `230-235`, `243-247`, `266-267`, `273`, `280-281`.

## Scope

Bring `src/usage.py` to 100% line coverage by extending `tests/test_usage.py`.

## Missing blocks

- Lines 230-235 (6 lines): error path in one provider
- Lines 243-247 (5 lines): error path in another provider
- Many singletons and short blocks: HTTP error handling, cache-miss fallbacks

## Triage

Provider paths are real API interactions. All should be tested via `httpx` AsyncClient mocking.

## Testing approach

`httpx` mocking is already in place in existing tests. Extend with:
- Network error simulations (timeout, connection refused)
- Non-JSON response bodies
- Missing fields in API responses
- 429 rate-limit responses
- 401 auth-failure responses

## Tests to add

Targeted per missing line. Cover both provider classes (`OAuthUsageProvider`, `OpenAIUsageProvider`) with parameterized tests where appropriate.

## Restrictions

Standard: no source modifications except dead/pragma. Use `httpx` mock fixture — do NOT make real HTTP requests in tests.

## Success criteria

- `pytest tests/test_usage.py --cov=src/usage --cov-report=term-missing --cov-fail-under=100` exits 0
- 100% coverage for `src/usage.py`
- All existing tests pass
- `ruff check src tests` exits 0
- `scripts/ci.sh` exits 0
