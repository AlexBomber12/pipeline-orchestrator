# PR-036: Dead code cleanup and CLAUDE.md fix

Branch: pr-036-dead-code-cleanup

## Scope

Remove dead code accumulated during sprint 4-5 MICRO PRs. Fix CLAUDE.md to be a 1-line reference instead of a full copy. Fix scaffolder to create CLAUDE.md for new repos.

## Changes

### src/daemon/runner.py

1. DELETE the entire `_commit_and_push_dirty` method (~152 lines). It is defined but never called from any production code path. References in docstrings of other methods should be updated to remove mentions.

2. Move `import re as _re` from inside `_mark_queue_done` (line ~1709) to the top-level imports as `import re`.

3. Update `_mark_queue_done` to use `re` instead of `_re`.

### tests/test_runner.py

DELETE all `_commit_and_push_dirty` test functions (~723 lines):
- test_commit_and_push_dirty_commits_when_dirty
- test_commit_and_push_dirty_skips_when_clean
- test_commit_and_push_dirty_errors_on_ci_failure
- test_commit_and_push_dirty_errors_when_ci_script_missing
- test_commit_and_push_dirty_gives_ci_script_generous_timeout
- test_commit_and_push_dirty_refuses_base_branch_as_expected
- test_commit_and_push_dirty_errors_when_head_on_wrong_branch

And any other tests that reference `_commit_and_push_dirty`.

### CLAUDE.md

Replace entire content with exactly:
```
Read and follow AGENTS.md in this repository.
```

### src/web/templates/components/stats_cards.html

DELETE this file. It is not referenced by any template or route.

### src/daemon/scaffolder.py

In `scaffold_repo`, after creating AGENTS.md, also create CLAUDE.md with content "Read and follow AGENTS.md in this repository." if CLAUDE.md does not exist.

### templates/

Add `templates/CLAUDE.md` with content:
```
Read and follow AGENTS.md in this repository.
```

### src/daemon/runner.py (_repo_looks_scaffolded)

Add CLAUDE.md check alongside AGENTS.md check:
```python
has_claude_md = (path / "CLAUDE.md").exists()
```
Include in the return condition. Scaffolding is complete when BOTH AGENTS.md (or CLAUDE.md legacy) AND CLAUDE.md exist.

## Success criteria
- scripts/ci.sh exits 0
- No references to _commit_and_push_dirty in src/ (only in git history)
- CLAUDE.md is 1 line
- stats_cards.html deleted
- Scaffolder creates CLAUDE.md for new repos
- runner.py ~200 lines shorter
- test_runner.py ~700 lines shorter
