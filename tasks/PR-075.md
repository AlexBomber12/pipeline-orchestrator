# PR-075: Codex coder plugin

Branch: pr-075-codex-plugin
- Type: architecture
- Complexity: low
- Depends on: PR-073
- Priority: 2
- Coder: any

## Problem

Same as PR-074 but for Codex CLI.

## Scope

Create CodexPlugin implementing CoderPlugin. Wraps existing codex_cli functions.

## Changes

### src/coders/codex.py (new)

```python
class CodexPlugin:
    name = "codex"
    display_name = "Codex CLI"
    models = ["", "gpt-5.4", "gpt-5.3-codex", "gpt-5.3-codex-spark", "gpt-5.2-codex", "gpt-5.4-mini", "gpt-5.1-codex-max", "gpt-5.1-codex-mini", "gpt-5.2"]
    # "" = default (CLI picks based on subscription)

    async def run_planned_pr(self, repo_path, model, timeout):
        return await codex_cli.run_planned_pr_async(repo_path, model=model or None, timeout=timeout)

    async def fix_review(self, repo_path, model, timeout):
        return await codex_cli.fix_review_async(repo_path, model=model or None, timeout=timeout)

    def check_auth(self) -> dict[str, str]:
        # extract existing codex auth check logic
        ...

    def create_usage_provider(self, **kwargs):
        return OpenAIUsageProvider(...)

    def rate_limit_patterns(self):
        # Codex retry regex, "usage limit" patterns
        ...
```

Note: first entry in models is "" (empty string) displayed as "(default)" in UI.

### tests/test_codex_plugin.py (new)

- test_codex_plugin_name
- test_codex_plugin_models_includes_default
- test_codex_plugin_run_planned_pr_delegates

## Success criteria
- scripts/ci.sh exits 0
- CodexPlugin implements CoderPlugin protocol
- Models list matches available OpenAI Codex models
- Tests pass
