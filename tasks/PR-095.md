# PR-095: 100% coverage for src/utils.py

Branch: pr-095-coverage-utils
- Type: refactor
- Complexity: low
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/utils.py` is at 87% line coverage with 2 missing lines: `19` and `24`. Module is 15 lines total.

## Scope

Bring `src/utils.py` to 100% line coverage by adding targeted tests in `tests/test_utils.py`. Create the test file if it does not exist.

## Missing lines at time of task creation

- Line 19: `return parts[-1]` inside `repo_slug_from_url()`, taken when URL has fewer than 2 slash-separated parts (bare name case)
- Line 24: `return repo_slug_from_url(url)` inside `repo_name_from_url()`, a deprecated wrapper

Both are real paths, not dead code. Must be tested, not deleted.

## Triage

- Line 19: write a test
- Line 24: write a test

## Tests to add in `tests/test_utils.py`

- `test_repo_slug_from_url_handles_bare_name`: pass a bare name like `"myrepo"` with no slashes, assert return value is `"myrepo"`.
- `test_repo_slug_from_url_handles_single_segment_with_colon`: edge case like `"myrepo"` after colon stripping.
- `test_repo_name_from_url_delegates_to_slug`: pass a well-formed URL, assert `repo_name_from_url()` returns the same value as `repo_slug_from_url()`.

## Restrictions

- Do NOT modify `src/utils.py`.
- Do NOT modify any other source module.
- Do NOT add new features.

## Success criteria

- `pytest tests/test_utils.py --cov=src/utils --cov-report=term-missing --cov-fail-under=100` exits 0.
- Overall coverage for `src/utils.py` is 100%.
- All existing tests still pass.
- `ruff check src tests` exits 0.
- `scripts/ci.sh` exits 0.
- No diff in `src/utils.py`.
