# PR-110: 100% coverage for src/github_client.py

Branch: pr-110-coverage-github-client
- Type: refactor
- Complexity: medium
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/github_client.py` is at 83% line coverage with 73 missing lines. Module wraps `gh` CLI for all GitHub API interactions.

Missing lines: `77`, `166-196`, `245`, `287`, `292`, `417`, `423`, `454`, `490`, `516`, `543`, `553-554`, `579-580`, `584`, `596-597`, `637-643`, `665-682`, `687`, `717-720`, `734`, `736`, `803`, `821`, `825`, `842-845`, `858-872`, `880-881`.

## Scope

Bring `src/github_client.py` to 100% line coverage by extending `tests/test_github_client.py` (already 2327 lines, extensive).

Expect 4-6 hours.

## Missing blocks at time of task creation

Three significant blocks plus scattered single lines:
- Lines 166-196: 31-line block, likely an entire helper function or parsing path
- Lines 637-643: 7-line block
- Lines 665-682: 18-line block
- Lines 858-872: 15-line block

Many scattered single lines likely error branches: gh CLI failures, JSON decode errors, missing fields in API responses.

## Triage

All blocks are real API error paths. Most should be tested. Rare case: if a branch checks for an API response format that was deprecated, consider deletion.

## Testing approach

`tests/test_github_client.py` already uses extensive `run_gh` monkeypatching. Extend with:
- New test cases that return empty responses, malformed JSON, specific error strings
- Tests for the large blocks (166-196, 637-643, 665-682, 858-872) by mocking specific subprocess outputs

## Tests to add in `tests/test_github_client.py`

Indicative:
- `test_<function_at_line_166>_handles_<scenario>`: one or more tests to cover 166-196 block — read the source to identify the function
- `test_is_pr_merged_<edge_cases>`: ensure all three branches (True/False/None) covered by existing tests; add if gaps
- `test_get_pr_review_status_<edge_cases>`: for missing lines 637-643 and others
- `test_<late_function>_<scenario>` for 665-682 and 858-872 blocks

## Restrictions

- Do NOT modify `src/github_client.py` except dead code / pragma with inline comments
- Do NOT modify any other source module
- Do NOT shell out to real `gh` CLI in tests — always mock via `run_gh` monkeypatch

## Success criteria

- `pytest tests/test_github_client.py --cov=src/github_client --cov-report=term-missing --cov-fail-under=100` exits 0
- Overall coverage for `src/github_client.py` is 100%
- All existing 800+ github_client tests still pass
- `ruff check src tests` exits 0
- `scripts/ci.sh` exits 0
- No diff in `src/github_client.py` except justified deletions/pragmas
