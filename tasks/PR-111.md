# PR-111: 100% coverage for src/daemon/scaffolder.py

Branch: pr-111-coverage-scaffolder
- Type: refactor
- Complexity: medium
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/daemon/scaffolder.py` is at 75% line coverage with 37 missing lines. Module handles repo cloning and scaffolding on first connect.

Missing ranges: `86-111`, `183-189`, `192-197`, `200-206`, `330-336`.

Large block 86-111 (26 lines) is likely the main scaffold flow with gh clone + initial setup. Blocks 183-206 (spanning) are likely error recovery paths.

## Scope

Bring `src/daemon/scaffolder.py` to 100% line coverage by adding tests in `tests/test_scaffolder.py` (create if it does not exist).

## Missing blocks

- Lines 86-111: main scaffold flow (clone, init, initial config)
- Lines 183-189, 192-197, 200-206: three related blocks, likely error-recovery variants (dirty tree recovery, branch verification, etc.)
- Lines 330-336: late cleanup or validation path

## Triage

All likely real scaffold flows. Test rather than delete.

## Testing approach

Scaffolder uses `subprocess` for git commands. Mock via `monkeypatch` of `_git` helper or `subprocess.run`.

Use `tmp_path` fixture for repo_path. Verify:
- Directory structure created correctly
- Branch setup correct
- Initial scaffolding files present/absent per invariants

## Tests to add in `tests/test_scaffolder.py`

- `test_scaffold_repo_happy_path`: main flow covering 86-111
- `test_scaffold_repo_handles_existing_dirty_tree`: one of 183-206 blocks
- `test_scaffold_repo_handles_branch_missing`: another of 183-206 blocks
- `test_scaffold_repo_handles_<third_variant>`: third block
- `test_scaffold_repo_cleanup_path` for 330-336

## Restrictions

Standard: do not modify source except dead code/pragma. No shell-outs.

## Success criteria

- `pytest tests/test_scaffolder.py --cov=src/daemon/scaffolder --cov-report=term-missing --cov-fail-under=100` exits 0
- 100% coverage for `src/daemon/scaffolder.py`
- All existing tests pass
- `ruff check src tests` exits 0
- `scripts/ci.sh` exits 0
