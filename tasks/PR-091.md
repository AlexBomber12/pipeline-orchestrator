# PR-091: Coder fallback policy with eligibility and priority

Branch: pr-091-fallback-policy
- Type: feature
- Complexity: medium
- Depends on: PR-077
- Priority: 2
- Coder: any

## Problem

Registry (PR-073) and plugin protocol (PR-077) are designed for N coders; Gemini/Qwen/future plugins are planned within 1-3 months. A hard-coded if-chain "on Claude rate limit try Codex" works for 2 coders but does not extend: each new coder would require rewriting the fallback cascade. We need a selector that takes the current system state and returns a ranked list of eligible coders, regardless of how many are registered.

Secondary goal: prevent data starvation during Phase 4 quality routing. If the selector is pure greedy (always pick top priority), the runner-up coder never accumulates metrics after a small initial lead, and the selector cannot learn whether the runner-up improved or would be better on some task types. Epsilon-greedy exploration solves this from day one without waiting for Phase 4.

Tertiary goal: initial priority values should reflect published benchmark performance of each coder rather than arbitrary numbers. This gives the selector a reasonable prior before any local data accumulates.

## Scope

Introduce `src/daemon/selector.py` with three pure functions:
- `eligible_coders(ctx) -> list[str]` filters out coders that cannot currently run (rate-limited, auth failed, pinned out by repo config, explicitly disabled for repo)
- `rank_coders(eligible, ctx) -> list[str]` ranks eligible coders; with probability `exploration_epsilon` returns a randomly-shuffled order, otherwise sorts by `coder_priority` descending
- `select_coder(ctx) -> tuple[str, CoderPlugin] | None` composes both and returns the top pick

`rank_coders` seeds randomness from `ctx.random` (injected) so tests are deterministic with a fixed seed.

Wire `select_coder` into `_get_coder` in runner. Keep `auto_fallback` config flag behaviour but implement through selector, not hard-coded branch. Static priority comes from `config.yml`; metric-based time-weighted scoring is a future extension (Phase 4), not scoped here.

Create `docs/CODER_PRIORS.md` documenting source data and initial `coder_priority` values. This is a living document that tracks where priors came from so they can be re-derived when benchmarks update.

## Changes

### src/daemon/selector.py (new)

```
from __future__ import annotations
import random
from dataclasses import dataclass
from src.coder_registry import CoderRegistry, CoderPlugin
from src.config import AppConfig, RepoConfig
from src.models import RepoState


@dataclass
class SelectionContext:
    registry: CoderRegistry
    repo_config: RepoConfig
    app_config: AppConfig
    state: RepoState
    rng: random.Random


def eligible_coders(ctx: SelectionContext) -> list[str]:
    pinned = ctx.repo_config.coder
    if pinned is not None and not ctx.app_config.daemon.auto_fallback:
        return [pinned.value]
    result: list[str] = []
    for name in ctx.registry.coder_names():
        if _is_rate_limited(name, ctx.state):
            continue
        if _auth_failed(name, ctx.registry):
            continue
        if _is_disabled_for_repo(name, ctx.repo_config):
            continue
        result.append(name)
    if pinned is not None and pinned.value in result:
        return [pinned.value] + [n for n in result if n != pinned.value]
    return result


def rank_coders(eligible: list[str], ctx: SelectionContext) -> list[str]:
    if len(eligible) <= 1:
        return eligible
    epsilon = ctx.app_config.daemon.exploration_epsilon
    if ctx.rng.random() < epsilon:
        shuffled = list(eligible)
        ctx.rng.shuffle(shuffled)
        return shuffled
    def priority(name: str) -> int:
        return ctx.app_config.daemon.coder_priority.get(name, 0)
    return sorted(eligible, key=priority, reverse=True)


def select_coder(ctx: SelectionContext) -> tuple[str, CoderPlugin] | None:
    eligible = eligible_coders(ctx)
    if not eligible:
        return None
    name = rank_coders(eligible, ctx)[0]
    return name, ctx.registry.get(name)


def _is_rate_limited(name: str, state: RepoState) -> bool:
    if state.rate_limit_reactive_coder == name:
        return True
    limited = getattr(state, "rate_limited_coders", None) or set()
    return name in limited


def _auth_failed(name: str, registry: CoderRegistry) -> bool:
    try:
        status = registry.get(name).check_auth()
    except Exception:
        return True
    return status.get("status") == "failed"


def _is_disabled_for_repo(name: str, repo_config: RepoConfig) -> bool:
    return name in (repo_config.disabled_coders or [])
```

### src/config.py

Add to `DaemonConfig`:
```
coder_priority: dict[str, int] = field(default_factory=lambda: {"codex": 10, "claude": 8})
exploration_epsilon: float = 0.15
```

Note on defaults: `{"codex": 10, "claude": 8}` is a placeholder close to neutral; the actual values must be reviewed against `docs/CODER_PRIORS.md` during implementation and adjusted to reflect current published benchmarks (SWE-bench Verified, Aider leaderboard, LiveCodeBench). Do not keep arbitrary 10/5 numbers from drafts.

Add both keys to `_DAEMON_FIELDS` with validation: `exploration_epsilon` in range `[0.0, 0.5]`.

Add to `RepoConfig` (optional):
```
disabled_coders: list[str] | None = None
```

### src/daemon/runner.py

Import `select_coder`, `SelectionContext`, and `random` at top. Add to runner init:
```
self._selector_rng = random.Random()
```

Replace `_get_coder` body:
```
def _get_coder(self) -> tuple[str, CoderPlugin]:
    ctx = SelectionContext(
        registry=self._registry,
        repo_config=self.repo_config,
        app_config=self.app_config,
        state=self.state,
        rng=self._selector_rng,
    )
    result = select_coder(ctx)
    if result is not None:
        return result
    coder = self.repo_config.coder or self.app_config.daemon.coder
    name = coder.value if hasattr(coder, "value") else str(coder)
    return name, self._registry.get(name)
```

### src/daemon/rate_limit.py

In reactive rate-limit detection, when a coder is determined rate-limited, add its name to `state.rate_limited_coders` (new field on `RepoState`) instead of executing any hard-coded fallback branch. Selector handles "switch to next eligible" on the next `_get_coder` call. Remove any draft chain logic from prior PR-091 sketch (e.g. `_fallback_coder` attribute).

### src/models.py

Add field to `RepoState`:
```
rate_limited_coders: set[str] = field(default_factory=set)
```

Ensure it is serialized and restored via Redis like other state fields.

### src/web/templates/components/settings_daemon.html

Keep the existing `auto_fallback` checkbox (unchanged semantics). Add a small numeric input for `exploration_epsilon` with hint: "Probability 0-0.5 that selector picks a non-top eligible coder to accumulate comparison data. Default 0.15 (roughly 1 in 7 selections). Set to 0 to disable exploration."

### docs/CODER_PRIORS.md (new)

Template skeleton documenting source data:
```
# Coder priority priors

Initial `coder_priority` values used by selector before local metrics accumulate.
Values are revisited every 2-3 months or when a coder publishes a new major model.

## Sources
- SWE-bench Verified (https://www.swebench.com/) last fetched: <date>
- Aider leaderboard last fetched: <date>
- LiveCodeBench last fetched: <date>

## Current priors
| Coder  | Priority | Rationale |
|--------|----------|-----------|
| codex  | TBD      | TBD based on latest SWE-bench Verified |
| claude | TBD      | TBD based on latest SWE-bench Verified |

## Methodology
Normalize each benchmark to 0-100 scale, average across benchmarks, round to nearest integer. Ties broken by Aider leaderboard.

## Revision log
- <date>: initial values sourced from public leaderboards
```

Implementer fills in TBD with current values at PR time.

### tests/test_selector.py (new)

- test_single_eligible_returns_self
- test_rate_limited_excluded
- test_auth_failed_excluded
- test_pinned_without_fallback_returns_only_pinned
- test_pinned_with_fallback_returns_pinned_first_then_others
- test_priority_ranks_higher_first_when_no_exploration: use rng seeded so exploration never fires
- test_disabled_for_repo_excluded
- test_no_eligible_returns_none
- test_new_plugin_added_without_selector_changes
- test_epsilon_zero_always_greedy
- test_epsilon_one_always_explores: non-greedy order at least once in N trials
- test_epsilon_seed_deterministic: same rng seed produces same selection sequence

### tests/test_runner.py

Add:
- test_get_coder_uses_selector
- test_get_coder_falls_through_to_default_when_selector_returns_none
- test_get_coder_auto_fallback_switches_on_rate_limit_via_selector
- test_get_coder_exploration_occasionally_picks_non_greedy

## Success criteria

- scripts/ci.sh exits 0
- Selector is a pure function module: no imports from runner, handlers, or rate_limit
- `_get_coder` contains no coder-specific branching
- Adding a new coder requires zero edits to selector.py (only a priority entry in config and plugin registration)
- With auto_fallback on and active coder rate-limited, next eligible coder is selected on next `_get_coder` call
- Repo-pinned coder with auto_fallback off never switches
- When no coder is eligible, selector returns None and runner falls through to configured default (pre-existing behaviour preserved)
- `exploration_epsilon=0` produces strictly greedy selection
- `exploration_epsilon=0.15` produces roughly 15% non-greedy selections over 200 trials (within statistical tolerance)
- `docs/CODER_PRIORS.md` exists with source references and current priors filled in from latest public benchmarks (not placeholder TBDs)
