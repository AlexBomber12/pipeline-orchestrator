# PR-101: 100% coverage for src/daemon/handlers/error.py

Branch: pr-101-coverage-handler-error
- Type: refactor
- Complexity: low
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/daemon/handlers/error.py` is at 95% line coverage with 9 missing lines: `90-91`, `156-159`, `180-185`, `195`, `280-285`. Module implements `ErrorHandler` for the ERROR state machine transition and the `diagnose_error` self-heal path.

## Scope

Bring `src/daemon/handlers/error.py` to 100% line coverage by adding targeted tests in `tests/test_error_handler.py` (create file if it does not exist).

## Missing lines at time of task creation

- Lines 90-91: `except Exception: snapshot = None` when `_claude_usage_provider.fetch()` raises during the rate-limit check
- Lines 156-159: `if code != 0:` branch — `diagnose_error` CLI failed, log event and return (no verdict parsing)
- Lines 180-185: `except (CalledProcessError, TimeoutExpired, OSError): head_before = ""` in the dirty-tree post-diagnose cleanup path
- Line 195: `branch = self.state.current_task.branch` when task has a non-default branch and no current_pr
- Lines 280-285: another branch in the dirty-tree/branch resolution path (look at current code for exact context)

All lines are legitimate error handling or state variations. Must be tested.

## Triage

All lines: write tests. None are dead code.

## Tests to add in `tests/test_error_handler.py`

- `test_handle_error_proceeds_when_usage_snapshot_fetch_raises`: mock `_claude_usage_provider.fetch()` to raise `RuntimeError`, assert diagnose still runs (snapshot falls back to None, not passing rate-limit pause).
- `test_handle_error_logs_and_returns_when_diagnose_cli_fails`: mock `diagnose_error_async` to return `(code=1, stdout="", stderr="boom")`, assert event log contains `"diagnose_error CLI failed"` and state remains ERROR.
- `test_handle_error_head_before_defaults_empty_when_rev_parse_fails`: in the dirty-tree + FIX verdict path, monkeypatch `git_ops._git` for `rev-parse HEAD` to raise `CalledProcessError`, assert the handler continues (uses `head_before=""`).
- `test_handle_error_uses_current_task_branch_when_no_current_pr_and_task_branch_differs`: state with `current_pr=None`, `current_task.branch="feature-x"` not matching `repo_config.branch`. Assert branch resolution picks `current_task.branch`.
- Lines 280-285: read actual code block, add corresponding targeted test. Likely an additional failure-recovery branch.

## Restrictions

- Do NOT modify `src/daemon/handlers/error.py`.
- Do NOT modify any other source module.
- Use pytest `monkeypatch` or `unittest.mock.AsyncMock` for async paths.
- Use `caplog` fixture to assert on log output where needed.

## Success criteria

- `pytest tests/test_error_handler.py --cov=src/daemon/handlers/error --cov-report=term-missing --cov-fail-under=100` exits 0.
- Overall coverage for `src/daemon/handlers/error.py` is 100%.
- All existing tests still pass.
- `ruff check src tests` exits 0.
- `scripts/ci.sh` exits 0.
- No diff in `src/daemon/handlers/error.py`.
