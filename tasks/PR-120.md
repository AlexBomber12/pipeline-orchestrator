# PR-120: 100% coverage for src/daemon/handlers/idle.py

Branch: pr-120-coverage-handler-idle
- Type: refactor
- Complexity: medium
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/daemon/handlers/idle.py` is at 91% line coverage with 34 missing lines. Handler implements IDLE state transition including auto-queue integration and QUEUE.md auto-generation (PR-087, PR-088).

Missing lines: `153-154`, `176`, `253-255`, `274`, `343-344`, `358`, `382-383`, `387-395`, `399-400`, `402-412`, `472-475`, `673-674`, `684-685`.

## Scope

Bring `src/daemon/handlers/idle.py` to 100% line coverage by extending existing idle-handler tests (search for them in tests/).

## Missing blocks

- Lines 387-395 (9 lines): LARGE block, likely specific auto-queue path
- Lines 402-412 (11 lines): LARGE block, possibly QUEUE.md generation path or error handling
- Lines 472-475 (4 lines): block
- Smaller scattered branches

## Triage

Likely all test-addable. IDLE handler has many conditional paths for different state combinations (has task/no task, has deps/no deps, etc.).

## Testing approach

Construct specific RepoState + tasks directory + git state combinations. Use `tmp_path` for filesystem operations, fake Redis, monkeypatched github_client.

For QUEUE.md generation path — verify generated content matches expected format.

## Tests to add

- Per large block, 1-2 tests exercising specific state combination
- Per small branch, 1 targeted test

## Restrictions

Standard: no source modifications except dead/pragma. No shell-outs to real git.

## Success criteria

- `pytest tests/test_idle_handler.py --cov=src/daemon/handlers/idle --cov-report=term-missing --cov-fail-under=100` (or equivalent test file name) exits 0
- 100% coverage for `src/daemon/handlers/idle.py`
- All existing tests pass
- `ruff check src tests` exits 0
- `scripts/ci.sh` exits 0
