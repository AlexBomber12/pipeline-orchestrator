# PR-100: 100% coverage for src/daemon/git_ops.py

Branch: pr-100-coverage-git-ops
- Type: refactor
- Complexity: low
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/daemon/git_ops.py` is at 81% line coverage with 9 missing lines: `96`, `106`, `123-128`, `131-137`, `160-161`. All missing lines are in `_base_branch_ahead_of_origin()` and `_working_tree_dirty()` error paths.

## Scope

Bring `src/daemon/git_ops.py` to 100% line coverage by adding targeted tests in `tests/test_git_ops.py` (create file if it does not exist).

## Missing lines at time of task creation

- Line 96: `_base_branch_ahead_of_origin` returns `True` when local branch `refs/heads/{branch}` is not found (local `rev-parse --verify` returncode != 0)
- Line 106: same function returns `True` when remote branch `refs/remotes/origin/{branch}` is not found
- Lines 123-128: same function returns `True` with warning log when `rev-list` probe fails (returncode != 0)
- Lines 131-137: same function `ValueError` except branch — when `rev-list` stdout is non-integer, logs warning and returns `True`
- Lines 160-161: `_working_tree_dirty` exception branch — `except (CalledProcessError, TimeoutExpired): return False`

All are legitimate error-handling paths. Must be tested via `monkeypatch` of `_git` subprocess helper.

## Triage

All lines: write tests. None are dead code.

## Tests to add in `tests/test_git_ops.py`

- `test_base_branch_ahead_returns_true_when_local_branch_missing`: monkeypatch `_git` so local `rev-parse` returns non-zero, assert result is `True`.
- `test_base_branch_ahead_returns_true_when_remote_branch_missing`: local OK, remote `rev-parse` non-zero, assert `True`.
- `test_base_branch_ahead_returns_true_when_rev_list_fails`: both rev-parses OK, `rev-list` returncode != 0, assert `True` and warning logged (use `caplog`).
- `test_base_branch_ahead_returns_true_on_non_integer_rev_list_output`: rev-list succeeds but stdout is `"not-a-number\n"`, assert `True` and warning logged.
- `test_base_branch_ahead_returns_true_on_timeout`: monkeypatch `_git` to raise `subprocess.TimeoutExpired`, assert `True`.
- `test_working_tree_dirty_returns_false_on_called_process_error`: monkeypatch `_git` to raise `CalledProcessError`, assert result is `False`.
- `test_working_tree_dirty_returns_false_on_timeout`: monkeypatch `_git` to raise `TimeoutExpired`, assert `False`.

## Restrictions

- Do NOT modify `src/daemon/git_ops.py`.
- Do NOT modify any other source module.

## Success criteria

- `pytest tests/test_git_ops.py --cov=src/daemon/git_ops --cov-report=term-missing --cov-fail-under=100` exits 0.
- Overall coverage for `src/daemon/git_ops.py` is 100%.
- All existing tests still pass.
- `ruff check src tests` exits 0.
- `scripts/ci.sh` exits 0.
- No diff in `src/daemon/git_ops.py`.
