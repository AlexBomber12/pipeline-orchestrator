# PR-074: Claude coder plugin

Branch: pr-074-claude-plugin
- Type: architecture
- Complexity: low
- Depends on: PR-073
- Priority: 2
- Coder: any

## Problem

Claude Code coder logic is spread across claude_cli.py, runner.py, rate_limit.py, settings_auth.html. Need to consolidate into a plugin.

## Scope

Create ClaudePlugin implementing CoderPlugin. Wraps existing claude_cli functions. Does NOT change runner yet.

## Changes

### src/coders/__init__.py (new)

Empty init.

### src/coders/claude.py (new)

```python
class ClaudePlugin:
    name = "claude"
    display_name = "Claude Code"
    models = ["opus", "sonnet"]

    async def run_planned_pr(self, repo_path, model, timeout):
        return await claude_cli.run_planned_pr_async(repo_path, model=model, timeout=timeout)

    async def fix_review(self, repo_path, model, timeout):
        return await claude_cli.fix_review_async(repo_path, model=model, timeout=timeout)

    def check_auth(self) -> dict[str, str]:
        # extract existing auth check logic from settings_auth endpoint
        ...

    def create_usage_provider(self, **kwargs):
        return OAuthUsageProvider(...)

    def rate_limit_patterns(self):
        # Anthropic percentage regex
        ...
```

### tests/test_claude_plugin.py (new)

- test_claude_plugin_name
- test_claude_plugin_models
- test_claude_plugin_run_planned_pr_delegates
- test_claude_plugin_check_auth

## Success criteria
- scripts/ci.sh exits 0
- ClaudePlugin implements CoderPlugin protocol
- Wraps existing claude_cli without duplicating logic
- Tests pass
