# PR-102: 100% coverage for src/queue_parser.py

Branch: pr-102-coverage-queue-parser
- Type: refactor
- Complexity: low
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/queue_parser.py` is at 96% line coverage with 10 missing lines: `213`, `248-252`, `255`, `261`, `358`, `362`, `399`, `436`. Module parses `tasks/PR-*.md` headers and writes QUEUE.md.

## Scope

Bring `src/queue_parser.py` to 100% line coverage by adding targeted tests in `tests/test_queue_parser.py` and/or `tests/test_queue_parser_depends_validation.py`.

## Missing lines at time of task creation

Lines to investigate: `213`, `248-252`, `255`, `261`, `358`, `362`, `399`, `436`.

Probable character:
- Early-exit validation branches for malformed headers (missing required fields, invalid Type, invalid Priority range, missing Depends on format)
- Error-message formatting paths for QueueValidationError
- Fallback default values for optional fields

Read the actual source at these lines. For each, determine category:
1. Edge-case validation not yet tested — write a test with malformed task file
2. Truly unreachable after upstream validation — delete or pragma with comment
3. Legitimate fallback default — write a test

## Triage

Per missing line, apply the standard triage. Most likely all are test-addable validation paths.

## Tests to add in `tests/test_queue_parser.py` (or depends_validation sibling file)

Examples of what to add (adjust to actual line content):
- `test_parse_task_header_rejects_missing_branch_field`
- `test_parse_task_header_rejects_invalid_type_value`
- `test_parse_task_header_rejects_out_of_range_priority`
- `test_parse_task_header_handles_empty_depends_on`
- `test_parse_task_header_fallback_for_missing_coder_field`

Replace these examples with tests matching the actual uncovered code paths. Do not invent tests that hit different lines than the missing ones.

## Restrictions

- Do NOT modify `src/queue_parser.py`.
- Do NOT modify any other source module.
- Do NOT relax header validation rules.
- Do NOT add new validation rules (coverage task, not feature).

## Success criteria

- `pytest tests/test_queue_parser.py tests/test_queue_parser_depends_validation.py --cov=src/queue_parser --cov-report=term-missing --cov-fail-under=100` exits 0.
- Overall coverage for `src/queue_parser.py` is 100%.
- All existing tests still pass.
- `ruff check src tests` exits 0.
- `scripts/ci.sh` exits 0.
- No diff in `src/queue_parser.py`.
