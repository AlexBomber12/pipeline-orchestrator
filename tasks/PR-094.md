# PR-094: 100% coverage for src/metrics.py

Branch: pr-094-coverage-metrics
- Type: refactor
- Complexity: low
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/metrics.py` is at 97% line coverage with 2 missing lines: `60` and `70`. Sprint 9 target is 100% across the codebase; this module is one step away.

## Scope

Bring `src/metrics.py` to 100% line coverage by adding targeted tests. No production behavior changes. No changes to any file other than `tests/test_metrics.py`.

## Missing lines at time of task creation

- Line 60: `return None` inside `MetricsStore.get()` when Redis returns `None` for the record key
- Line 70: `return []` inside `MetricsStore.recent()` when `limit <= 0`

Both are legitimate real paths, not dead code. They must be tested, not deleted or pragma'd.

## Triage

For each missing line, choose exactly one:
1. **Write a test** that exercises the line (preferred; applies here for both)
2. **Delete as dead code** (not applicable here)
3. **`# pragma: no cover` with inline justification comment** (not applicable here)

## Tests to add in `tests/test_metrics.py`

- `test_get_returns_none_when_redis_has_no_record`: mock Redis `get` to return `None`, assert `MetricsStore.get()` returns `None`.
- `test_recent_returns_empty_list_when_limit_is_zero`: call `MetricsStore.recent(task_id="PR", limit=0)`, assert return value is `[]`, assert Redis `lrange` was not called.
- `test_recent_returns_empty_list_when_limit_is_negative`: same but with `limit=-1`.

## Restrictions

- Do NOT modify `src/metrics.py` (no pragma, no refactor, no new code).
- Do NOT modify any other source module.
- Do NOT add new features.
- Use the existing fake Redis fixture pattern already present in `tests/test_metrics.py`.

## Success criteria

- `pytest tests/test_metrics.py --cov=src/metrics --cov-report=term-missing --cov-fail-under=100` exits 0.
- Overall coverage for `src/metrics.py` is 100%.
- All existing tests in `tests/test_metrics.py` still pass.
- `ruff check src tests` exits 0.
- `scripts/ci.sh` exits 0.
- No diff in `src/metrics.py`.
- New tests have explicit assertions about behavior (not just coverage hits).
