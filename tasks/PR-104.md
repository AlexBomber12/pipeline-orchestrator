# PR-104: 100% coverage for src/daemon/main.py

Branch: pr-104-coverage-daemon-main
- Type: refactor
- Complexity: low
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/daemon/main.py` is at 93% line coverage with 12 missing lines: `73`, `110`, `123-124`, `168`, `257`, `262`, `307-308`, `336-337`, `373`. Module is the daemon entry point and top-level loop.

## Scope

Bring `src/daemon/main.py` to 100% line coverage by adding targeted tests in `tests/test_daemon_main.py`.

## Missing lines at time of task creation

Investigate the source at each line range and classify:
- Line 73: likely a config-loading or env-var fallback branch
- Line 110: likely an error path during daemon startup
- Lines 123-124: likely a 2-line except/recovery block
- Line 168: likely a conditional inside the main loop
- Line 257, 262: likely signal/shutdown handling branches
- Lines 307-308: likely error handling during repo iteration
- Lines 336-337: likely cleanup or teardown branch
- Line 373: likely CLI entry-point — check if this is `if __name__ == "__main__":` fallback

## Triage

Special consideration for Line 373: if it is `if __name__ == "__main__": main()` entry point, add `# pragma: no cover  # entry point invoked via python -m` with comment. All other lines should be tested.

## Tests to add in `tests/test_daemon_main.py`

- Test startup error paths via monkeypatch of config loading or Redis connection
- Test signal handler invocation (SIGTERM graceful shutdown)
- Test main loop iteration with error recovery
- Use `pytest.raises` for exception paths

Exact test names depend on actual source content at the missing lines.

## Restrictions

- Do NOT modify `src/daemon/main.py` except for pragma addition on line 373 if justified
- If pragma is added, it MUST have inline comment explaining why the line is unreachable in tests
- Do NOT modify any other source module

## Success criteria

- `pytest tests/test_daemon_main.py --cov=src/daemon/main --cov-report=term-missing --cov-fail-under=100` exits 0.
- Overall coverage for `src/daemon/main.py` is 100%.
- All existing tests still pass.
- `ruff check src tests` exits 0.
- `scripts/ci.sh` exits 0.
- Diff in `src/daemon/main.py` is either empty or exactly one pragma comment on line 373 (or equivalent entry-point line).
