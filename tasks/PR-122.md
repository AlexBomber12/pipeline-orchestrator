# PR-122: 100% coverage for src/daemon/handlers/watch.py

Branch: pr-122-coverage-handler-watch
- Type: refactor
- Complexity: low
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/daemon/handlers/watch.py` is at 87% line coverage with 13 missing lines. Handler implements WATCH state (polls PR status, transitions to FIX/IDLE/HUNG).

Missing lines: `25-27`, `34-38`, `114`, `144`, `149`, `173`, `175`.

## Scope

Bring `src/daemon/handlers/watch.py` to 100% line coverage by adding tests in `tests/test_watch_handler.py` (create if needed).

## Missing blocks

- Lines 25-27 (3 lines): early guard or short-circuit
- Lines 34-38 (5 lines): continuation block
- Scattered late lines: 114, 144, 149, 173, 175 — likely branches in dedup and state-transition logic

## Triage

All likely test-addable. WATCH has specific branches for:
- PR disappears (merged/closed externally) — lines may relate to this after PR-093 changes
- Review feedback detection
- CI failure detection
- HUNG detection based on timers

## Testing approach

Use fake `github_client.list_open_prs`, `get_pr_status`, `is_pr_merged` (from PR-093). Construct specific scenarios:
- PR no longer in open list, merged=True
- PR no longer in open list, merged=False
- PR no longer in open list, merged=None (unknown)
- Review feedback received
- CI failure observed
- Timer exceeded (HUNG transition)

## Tests to add

Per missing branch, one targeted test. Reference PR-093 test patterns where applicable.

## Restrictions

Standard: no source modifications except dead/pragma.

## Success criteria

- `pytest tests/test_watch_handler.py --cov=src/daemon/handlers/watch --cov-report=term-missing --cov-fail-under=100` exits 0
- 100% coverage for `src/daemon/handlers/watch.py`
- All existing tests pass
- `ruff check src tests` exits 0
- `scripts/ci.sh` exits 0
