# PR-121: 100% coverage for src/daemon/handlers/coding.py

Branch: pr-121-coverage-handler-coding
- Type: refactor
- Complexity: low
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/daemon/handlers/coding.py` is at 87% line coverage with 16 missing lines. Handler implements CODING state (invokes coder plugin for planned PR).

Missing lines: `86`, `102-108`, `147-150`, `184-189`.

## Scope

Bring `src/daemon/handlers/coding.py` to 100% line coverage by extending existing coding-handler tests.

## Missing blocks

- Line 86: single branch
- Lines 102-108 (7 lines): medium block
- Lines 147-150 (4 lines): block
- Lines 184-189 (6 lines): block

## Triage

All likely test-addable. CODING handler branches are typically: pre-flight checks, coder invocation, post-invocation verification.

## Testing approach

Mock coder plugin's `run_planned_pr` to return specific outcomes (success, timeout, rate-limited, error). Verify state transitions.

Mock `github_client` for PR creation verification. Mock `git_ops` for push-result verification.

## Tests to add

Per missing block, one targeted test exercising that specific scenario.

## Restrictions

Standard: no source modifications except dead/pragma.

## Success criteria

- `pytest tests/test_coding_handler.py --cov=src/daemon/handlers/coding --cov-report=term-missing --cov-fail-under=100` (or equivalent) exits 0
- 100% coverage for `src/daemon/handlers/coding.py`
- All existing tests pass
- `ruff check src tests` exits 0
- `scripts/ci.sh` exits 0
