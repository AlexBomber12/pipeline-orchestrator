# PR-096: 100% coverage for src/daemon/selector.py

Branch: pr-096-coverage-selector
- Type: refactor
- Complexity: low
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/daemon/selector.py` is at 97% line coverage with 3 missing lines: `47`, `142`, `143`. Module is 153 lines with already comprehensive test suite in `tests/test_selector.py` (19 tests).

## Scope

Bring `src/daemon/selector.py` to 100% line coverage by adding targeted tests in `tests/test_selector.py`.

## Missing lines at time of task creation

- Line 47: `continue` inside `eligible_coders()` that skips a coder when `_is_disabled_for_repo(name, ctx.repo_config)` returns True (disabled via `repo_config.disabled_coders`)
- Lines 142-143: `except Exception: return True` inside `_auth_failed()`, triggered when `registry.get(name).check_auth()` raises any exception

Both are real paths. Line 47 may overlap with existing `test_disabled_for_repo_excluded` (verify the test actually hits this specific line, not a different disabled-check path). Lines 142-143 exception path likely needs a fresh test.

## Triage

- Line 47: check if existing test covers it; if not, add or extend a test
- Lines 142-143: write a new test

## Tests to add or verify in `tests/test_selector.py`

- `test_disabled_coder_in_repo_config_excluded_from_eligible`: create a `RepoConfig` with `disabled_coders=["claude"]`, assert `eligible_coders()` does not include `"claude"` even if otherwise eligible. If an equivalent test already exists, ensure it covers line 47 specifically.
- `test_auth_failed_returns_true_when_check_auth_raises`: create a registry where `get("claude").check_auth()` raises a `RuntimeError` (or any exception), pass `auth_statuses=None` so the registry path is taken, assert `_auth_failed("claude", registry, None)` returns `True`. Use `monkeypatch` or a fake plugin.

## Restrictions

- Do NOT modify `src/daemon/selector.py`.
- Do NOT modify any other source module.
- Use the existing fixtures and fake-plugin patterns in `tests/test_selector.py`.

## Success criteria

- `pytest tests/test_selector.py --cov=src/daemon/selector --cov-report=term-missing --cov-fail-under=100` exits 0.
- Overall coverage for `src/daemon/selector.py` is 100%.
- All existing 19 tests still pass.
- `ruff check src tests` exits 0.
- `scripts/ci.sh` exits 0.
- No diff in `src/daemon/selector.py`.
