# PR-086: DAG builder and eligible task selector

Branch: pr-086-dag-builder
- Type: feature
- Complexity: medium
- Depends on: PR-085
- Priority: 2
- Coder: any

## Problem

Need to determine which tasks are eligible to run: all dependencies DONE, task itself TODO, sorted by priority.

## Scope

Build dependency DAG from task headers, select eligible tasks respecting priority order.

## Changes

### src/dag.py (new)

```python
def build_task_dag(headers: list[TaskHeader]) -> dict[str, list[str]]:
    """Build adjacency list from Depends on fields. Validate no cycles."""

def get_eligible_tasks(
    headers: list[TaskHeader],
    statuses: dict[str, TaskStatus],
) -> list[TaskHeader]:
    """Return tasks where status=TODO and all dependencies DONE.

    Sorted by Priority ASC (1=highest), then PR number ASC.
    """

def detect_cycle(dag: dict[str, list[str]]) -> list[str] | None:
    """Return cycle path if found, None otherwise. Iterative DFS."""
```

### tests/test_dag.py (new)

- test_eligible_respects_dependencies: PR-B depends on PR-A (DONE) -> PR-B eligible
- test_eligible_blocks_unmet_deps: PR-B depends on PR-A (TODO) -> PR-B not eligible
- test_eligible_sorted_by_priority: priority 1 before priority 3
- test_eligible_sorted_by_pr_number_within_priority
- test_cycle_detection
- test_no_cycle_in_valid_dag

## Success criteria
- scripts/ci.sh exits 0
- Eligible tasks correctly filtered and sorted
- Cycle detection prevents infinite loops
- All tests pass
