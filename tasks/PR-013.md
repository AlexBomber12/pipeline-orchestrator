# PR-013: Extract shared utils

Branch: pr-013-utils

## Scope

Extract `repo_name_from_url` (duplicated in runner.py and web/app.py) into a shared utils module. Update all imports.

## Files to create

### src/utils.py

Move from src/web/app.py:
- repo_name_from_url(url: str) -> str

Keep the logic identical. Single source of truth.

## Files to modify

### src/web/app.py
- Remove local repo_name_from_url function
- Add `from src.utils import repo_name_from_url`

### src/daemon/runner.py
- Remove local repo_name_from_url function
- Add `from src.utils import repo_name_from_url`

### tests/test_web_app.py
- Update import if tests reference repo_name_from_url from web.app

## Files to create

### tests/test_utils.py
- Move existing repo_name_from_url tests here
- Test with .git suffix, without, trailing slash, git@ SSH URL

## Success criteria
- scripts/ci.sh exits 0
- All existing tests still pass
- grep -r "def repo_name_from_url" src/ returns exactly 1 result (in utils.py)
