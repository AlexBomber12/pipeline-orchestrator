# PR-106: 100% coverage for src/daemon/recovery.py

Branch: pr-106-coverage-recovery
- Type: refactor
- Complexity: low
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/daemon/recovery.py` is at 88% line coverage with 13 missing lines: `99-104`, `219-223`, `275-276`, `279`, `284-287`. Module rebuilds daemon state from Redis + git + GitHub after restart.

## Scope

Bring `src/daemon/recovery.py` to 100% line coverage by adding targeted tests in `tests/test_recovery.py`.

## Missing lines at time of task creation

Investigate source at each range:
- Lines 99-104: 6-line block, likely an error path during state rehydration from Redis (JSON decode error, missing key, schema mismatch)
- Lines 219-223: 5-line block, likely GitHub API fallback when PR info cannot be fetched
- Lines 275-276: 2-line branch, possibly in current_task or current_pr reconstruction
- Line 279: single branch continuation
- Lines 284-287: 4-line block, likely final cleanup or default-state branch

## Triage

Recovery paths are defensive and real — daemon restarts happen in production. All lines should be test-addable. Delete only if line is truly unreachable (e.g., dead branch after refactoring left behind).

## Tests to add in `tests/test_recovery.py`

Examples — adjust to actual source content:
- `test_recover_state_returns_default_when_redis_payload_malformed_json`
- `test_recover_state_handles_redis_schema_mismatch_gracefully`
- `test_recover_state_falls_back_when_github_pr_lookup_fails`
- `test_recover_state_reconstructs_current_task_when_pr_missing`
- `test_recover_state_final_cleanup_clears_<something>_when_<condition>`

Use fake Redis and monkeypatched `github_client` functions for isolation.

## Restrictions

- Do NOT modify `src/daemon/recovery.py`.
- Do NOT modify any other source module.
- Do NOT change recovery semantics (stateless-invariant applies).

## Success criteria

- `pytest tests/test_recovery.py --cov=src/daemon/recovery --cov-report=term-missing --cov-fail-under=100` exits 0.
- Overall coverage for `src/daemon/recovery.py` is 100%.
- All existing tests still pass.
- `ruff check src tests` exits 0.
- `scripts/ci.sh` exits 0.
- No diff in `src/daemon/recovery.py`.
