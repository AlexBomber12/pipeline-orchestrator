# PR-107b: 100% coverage for merge._mark_queue_done

Branch: pr-107b-coverage-merge-mark-queue-done
- Type: refactor
- Complexity: medium
- Depends on: PR-107a
- Priority: 3
- Coder: any

## Problem

Second of three sub-PRs for `src/daemon/handlers/merge.py`. Covers `_mark_queue_done()` method at lines 174-294.

Missing lines in scope: `184`, `204-206`, `211-213`, `259-282`. Total ~30 missing lines.

Note: despite the function name referring to QUEUE.md editing (pre-PR-088 behavior), the function still exists for legacy flow handling. After PR-088 QUEUE.md is auto-generated, so some branches here may be legitimately unreachable dead code — flag them for removal rather than writing artificial tests.

## Scope

Bring `_mark_queue_done()` function branches to covered state. Do NOT touch `handle_merge` (107a handles) or `_resolve_pending_queue_sync`/`_escalate_queue_sync_if_expired` (107c).

Add tests in `tests/test_runner.py` per project convention.

## Missing blocks

- Line 184: post-resolution verification branch
- Lines 204-206: 3-line error classification after merge attempt
- Lines 211-213: 3-line paired logic, similar classification variant
- Lines 259-282: 24-line LARGE block — probably the full legacy queue-editing path

## Triage (IMPORTANT for this sub-PR)

The 259-282 block deserves careful review:
- If it handles legacy QUEUE.md editing that's now superseded by PR-088 auto-generation → candidate for DELETION with inline comment
- If it handles a distinct path (queue-sync PR creation for remediation) → must be tested

Read the surrounding docstring and invariants in DESIGN.md before deciding. When in doubt, prefer test over deletion (deletion reduces safety net).

Pragma allowed only for:
- Defensive branches that can only trigger if upstream invariants are violated
- Platform-specific paths (unlikely here)

## Testing approach

Mock:
- `git_ops._git` for queue file state simulation
- `github_client.open_pr` if queue-sync PRs are created
- `MetricsStore` for run record verification

Tests construct states matching legacy-queue or new-DAG modes based on what the 259-282 block actually does.

## Tests to add in `tests/test_runner.py`

Indicative:
- `test_mark_queue_done_verification_branch_at_184`
- `test_mark_queue_done_error_classification_204_206`
- `test_mark_queue_done_error_classification_211_213`
- 1-3 tests covering the 259-282 block depending on what it does

## Success criteria

- `pytest tests/test_runner.py --cov=src/daemon/handlers/merge --cov-report=term-missing` runs with exit 0
- Lines 184, 204-206, 211-213, 259-282 no longer appear in "Missing" column for merge.py (either tested or deleted as dead with PR description justification)
- Lines 35-145 remain covered (by PR-107a, verify no regression)
- Lines 295-353 MAY still appear in Missing (scope of PR-107c)
- All existing tests pass
- `ruff check src tests` exits 0
- `scripts/ci.sh` exits 0
- Any deletions in `src/daemon/handlers/merge.py` have clear PR description justification; pragma annotations have inline comments

## Restrictions

- Do NOT touch lines 32-173 (handled by 107a) or 295-353 (107c)
- Do NOT modify any other source module
- Do NOT introduce new features
- If deleting legacy queue-editing code: preserve the queue-sync PR creation logic (remediation for divergent main vs DAG) if that's what the block does
