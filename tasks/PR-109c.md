# PR-109c: 100% coverage for runner CLI log, waiting, run_cycle

Branch: pr-109c-coverage-runner-cycle
- Type: refactor
- Complexity: high
- Depends on: PR-109b
- Priority: 3
- Coder: any

## Problem

Third and final sub-PR for `src/daemon/runner.py`. Covers `_save_cli_log`, `log_event`, `_publish_while_waiting`, `run_cycle` (lines 514-634).

Missing lines in scope: `523-525`, `529-530`, `554`, `560-565`, `573`, `577-592`, `600-601`, `613`, `615`, `622-623`. Total ~36 missing lines including the largest remaining block 577-592.

After this PR lands, `src/daemon/runner.py` reaches 100% coverage.

## Scope

Bring CLI logging helpers and the main `run_cycle` state machine dispatcher to covered. This is the top-level loop that routes to handlers based on `state.state`.

Add tests in `tests/test_runner.py`.

## Missing blocks

- Lines 523-525: 3-line block in `_save_cli_log`, likely error path (write failure, fs error)
- Lines 529-530: paired in `_save_cli_log`, secondary branch
- Line 554: single branch in `_publish_while_waiting`, likely publish error handling
- Lines 560-565: 6-line block at start of `run_cycle`, likely state recovery or preflight branch
- Line 573: mid-run_cycle branch
- Lines 577-592: 16-line LARGE block, probably handler dispatch table with several state branches
- Lines 600-601: paired post-dispatch branch
- Line 613: late branch (cleanup or stats)
- Line 615: continuation
- Lines 622-623: paired final cleanup

## Triage

`run_cycle` is the core dispatch — every branch here drives which handler runs. All blocks are real operational paths.

The 577-592 block is particularly important — it's the handler dispatch. Each line likely corresponds to one state transition. Test each branch by constructing RepoState with that specific `.state` value and verifying the correct handler is invoked.

## Testing approach

Existing `tests/test_runner.py` has extensive run_cycle tests. Extend with:
- State values that trigger the specific untested dispatch branches
- Preflight error scenarios for 560-565
- Publish error simulation for 554
- CLI log save error paths (fs full, permission denied)

Mock handlers to return specific outcomes; assert on state transitions.

## Tests to add in `tests/test_runner.py`

- `test_save_cli_log_handles_write_error_523_525`
- `test_save_cli_log_secondary_branch_529_530`
- `test_publish_while_waiting_handles_error_at_554`
- `test_run_cycle_preflight_branch_560_565`
- `test_run_cycle_branch_at_573`
- Multiple `test_run_cycle_dispatches_<state>` tests for each branch in 577-592 block
- `test_run_cycle_post_dispatch_600_601`
- `test_run_cycle_late_branch_613`
- `test_run_cycle_late_branch_615`
- `test_run_cycle_final_cleanup_622_623`

## Success criteria

- `pytest tests/test_runner.py --cov=src/daemon/runner --cov-report=term-missing --cov-fail-under=100` exits 0
- **Overall coverage for `src/daemon/runner.py` is 100%** (last in chain)
- Lines from PR-109a and PR-109b scope remain covered
- All existing tests pass (890+)
- `ruff check src tests` exits 0
- `scripts/ci.sh` exits 0
- No diff in `src/daemon/runner.py` except justified deletions/pragmas

## Restrictions

- Do NOT touch lines 114-513 (109a and 109b handled)
- Do NOT modify any other source module
- Do NOT refactor `run_cycle` dispatch structure
- Do NOT change handler interfaces
- This is the CLOSING sub-PR for runner.py — MUST achieve 100%
