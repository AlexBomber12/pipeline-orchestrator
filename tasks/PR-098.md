# PR-098: 100% coverage for src/config.py

Branch: pr-098-coverage-config
- Type: refactor
- Complexity: low
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/config.py` is at 95% line coverage with 8 missing lines: `79`, `149`, `202-208`. Module has 154 statements, 303 lines total.

## Scope

Bring `src/config.py` to 100% line coverage by adding targeted tests in `tests/test_config.py`.

## Missing lines at time of task creation

- Line 79: `return 60` inside `_poll_interval_at_least_one` validator when `v is None` (accept None, fall back to default)
- Line 149: `daemon["fix_idle_timeout_sec"] = legacy` inside `load_config` when legacy `fix_review_timeout_sec` is present and new name is not
- Lines 202-208: `except Exception` branch in `save_config()` that unlinks the temp file on failure and re-raises

All three blocks are real error/migration paths. Must be tested.

## Triage

- Line 79: write a test passing `poll_interval_sec=None` (or omitting it) via model construction that triggers the validator
- Line 149: write a test loading a YAML with legacy `fix_review_timeout_sec` but no `fix_idle_timeout_sec`, verify migration occurred
- Lines 202-208: write a test that triggers the exception path (e.g., monkeypatch `os.replace` to raise), verify temp file is cleaned up and original exception propagates

## Tests to add in `tests/test_config.py`

- `test_poll_interval_validator_defaults_to_60_when_none`: verify the validator returns 60 when input is None. Construct `DaemonConfig` with `poll_interval_sec=None` or use the validator directly.
- `test_load_config_migrates_fix_review_timeout_sec_to_fix_idle_timeout_sec`: write a temp YAML with `daemon.fix_review_timeout_sec: 120` but no `fix_idle_timeout_sec`, call `load_config`, assert result has `fix_idle_timeout_sec == 120`.
- `test_load_config_does_not_override_existing_fix_idle_timeout_sec`: both legacy and new field present, assert new field value is preserved.
- `test_save_config_cleans_up_temp_file_on_os_replace_failure`: monkeypatch `os.replace` to raise `OSError`, call `save_config`, assert (a) `OSError` is raised, (b) the tmp file no longer exists in the target directory.
- `test_save_config_cleanup_handles_missing_tmp_file_gracefully`: monkeypatch `os.replace` to raise, and monkeypatch `os.unlink` to raise `FileNotFoundError` first time. Assert original `OSError` still propagates without masking.

## Restrictions

- Do NOT modify `src/config.py`.
- Do NOT modify any other source module.
- Use `tmp_path` pytest fixture for file-system tests.

## Success criteria

- `pytest tests/test_config.py --cov=src/config --cov-report=term-missing --cov-fail-under=100` exits 0.
- Overall coverage for `src/config.py` is 100%.
- All existing tests still pass.
- `ruff check src tests` exits 0.
- `scripts/ci.sh` exits 0.
- No diff in `src/config.py`.
