# PR-107c: 100% coverage for merge queue-sync helpers

Branch: pr-107c-coverage-merge-queue-sync
- Type: refactor
- Complexity: medium
- Depends on: PR-107b
- Priority: 3
- Coder: any

## Problem

Third and final sub-PR for `src/daemon/handlers/merge.py`. Covers `_resolve_pending_queue_sync()` (lines 295-338) and `_escalate_queue_sync_if_expired()` (lines 339-353).

Missing lines in scope: `300-337`, `340-353`. Total ~52 missing lines across 2 blocks.

After this PR lands, `src/daemon/handlers/merge.py` reaches 100% coverage.

## Scope

Bring both helper functions to covered state. These implement queue-sync remediation PR tracking (when daemon auto-commits conflict with main branch state, a remediation PR is opened and tracked for resolution / expiry).

Add tests in `tests/test_runner.py`.

## Missing blocks

- Lines 300-337: 38-line block covering most of `_resolve_pending_queue_sync()`. Logic: poll remediation PR status, mark as resolved when closed/merged, clear `pending_queue_sync_branch` state
- Lines 340-353: 14-line block covering most of `_escalate_queue_sync_if_expired()`. Logic: check if remediation PR exceeded timeout, escalate to ERROR state

## Triage

Both blocks are real operational paths — queue-sync remediation DOES happen when daemon's QUEUE.md auto-commit races with external changes. Test rather than delete.

Pragma only for defensive branches that cannot trigger given invariants.

## Testing approach

Mock `github_client` for PR status transitions:
- `get_pr_status` returns "open" → polling continues
- `get_pr_status` returns "closed"/"merged" → resolve path
- `get_pr_status` returns None / PR missing → handle gracefully

Mock `datetime.now` or set `pending_queue_sync_started_at` explicitly to test expiry branches.

State setup: construct `RepoState` with `pending_queue_sync_branch` set to specific branch name.

## Tests to add in `tests/test_runner.py`

Indicative:
- `test_resolve_pending_queue_sync_continues_when_pr_open`
- `test_resolve_pending_queue_sync_clears_state_when_pr_merged`
- `test_resolve_pending_queue_sync_clears_state_when_pr_closed`
- `test_resolve_pending_queue_sync_handles_missing_pr`
- `test_escalate_queue_sync_no_op_when_not_expired`
- `test_escalate_queue_sync_transitions_to_error_when_expired`
- `test_escalate_queue_sync_clears_state_after_escalation`

## Success criteria

- `pytest tests/test_runner.py --cov=src/daemon/handlers/merge --cov-report=term-missing --cov-fail-under=100` exits 0
- **Overall coverage for `src/daemon/handlers/merge.py` is 100%** (last in chain, final gate)
- All lines in PR-107a and PR-107b scope remain covered (verify no regression)
- All existing tests pass
- `ruff check src tests` exits 0
- `scripts/ci.sh` exits 0
- No diff in `src/daemon/handlers/merge.py` except justified deletions/pragmas

## Restrictions

- Do NOT touch lines 32-294 (handled by 107a and 107b)
- Do NOT modify any other source module
- Do NOT change queue-sync remediation timeout or escalation semantics
- This is the CLOSING sub-PR for merge.py — it MUST achieve 100%, no partial credit
