# PR-198: PipelineState.MERGE dead value cleanup

Branch: pr-198-pipeline-state-merge-cleanup
- Type: refactor
- Complexity: low
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`PipelineState.MERGE = "MERGE"` exists in `src/models.py:18`. Investigation needed to confirm if it is reachable code or dead value left over from earlier design. If dead, remove it cleanly so the state enum reflects only real states.

The current 7 work states are IDLE, CODING, WATCH, FIX, MERGE, ERROR, HUNG (per architectural decisions section of roadmap). If MERGE is one of the real 7, this PR is wrong and should be re-scoped. Verify before removing.

## Scope

Confirm whether MERGE is reachable. If unreachable, remove. If reachable, this PR is closed without changes (and roadmap line 1364 in F4.2 should be updated).

## Files to inspect

`src/models.py`:
- Line 18: `MERGE = "MERGE"`.

`src/daemon/`:
- grep for any code that sets `state = PipelineState.MERGE` or compares against it.

`src/web/`:
- grep for any UI rendering that handles MERGE specifically.

`src/daemon/handlers/merge.py`:
- This handler exists for the merge phase of the pipeline. Check if its name conflicts with the enum value confusingly.

## Investigation steps

```bash
grep -rn "PipelineState.MERGE\|state.*=.*MERGE\|state ==.*MERGE\|\"MERGE\"" src/ tests/
```

If only the enum definition matches → dead value, remove.

If real assignment or comparison sites exist → reachable, document the actual transitions in roadmap.md and close this PR.

## Files to modify (if dead)

`src/models.py`:
- Remove `MERGE = "MERGE"` line.

`tests/test_models.py` (or similar):
- Update enum tests if any list all values.

`docs/roadmap.md`:
- Update line that references "Clean up PipelineState.MERGE dead value" to status DONE.
- Confirm no other docs reference the dead value.

## Files to update (if reachable)

`docs/roadmap.md`:
- Update state machine section to clearly include MERGE as a real state.
- Update Sprint F4.2 PR-195 status to "REJECTED — value is reachable".

`docs/architecture-state-machine.md`:
- Document MERGE state semantics.

## Implementation notes

This is a verification-first task. Do NOT remove the value without first confirming dead status via grep. False removal would break runtime state transitions.

## Success criteria

1. Investigation result documented in PR description (dead OR reachable).
2. If dead: enum value removed, no test regressions, roadmap updated.
3. If reachable: PR closed without code changes, but roadmap docs updated to reflect MERGE as real state.
4. `python -m ruff check src/` passes.
5. `python -m pytest --cov=src --cov-fail-under=100` passes.
