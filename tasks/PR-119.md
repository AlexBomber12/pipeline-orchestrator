# PR-119: 100% coverage for src/web/app.py

Branch: pr-119-coverage-web-app
- Type: refactor
- Complexity: high
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/web/app.py` is at 87% line coverage with **106 missing lines** — the largest absolute gap. Module is the FastAPI dashboard with 40+ endpoints.

Missing lines (highly scattered): `77-81`, `133`, `182-183`, `274`, `277-278`, `280`, `287`, `290`, `292`, `296-299`, `329-331`, `356`, `443`, `604`, `721-722`, `764-773`, `877-878`, `933-934`, `936`, `1004-1005`, `1146`, `1153`, `1188`, `1209`, `1240`, `1242`, `1266-1270`, `1304`, `1336-1338`, `1441`, `1453-1454`, `1456`, `1517`, `1541`, `1546`, `1550`, `1557-1558`, `1606`, `1612`, `1615`, `1620-1621`, `1686`, `1694-1695`, `1704-1705`, `1719`, `1727`, `1751`, `1775-1776`, `1801-1802`, `1844-1853`, `1872-1873`, `1876-1888`.

## Scope

Bring `src/web/app.py` to 100% line coverage by extending `tests/test_web_app.py`, `tests/test_settings.py`, `tests/test_upload.py`, `tests/test_observability.py`, `tests/test_alerts.py`.

Expect 6-10 hours. Largest module but error paths are typically easy to trigger via FastAPI TestClient.

## Missing blocks

A few significant blocks:
- Lines 764-773 (10 lines): one endpoint error handler or response branch
- Lines 1266-1270 (5 lines): endpoint branch
- Lines 1844-1853 (10 lines): likely near the main entry point or startup error handler
- Lines 1872-1888 (17 lines): END of file, likely `if __name__ == "__main__": uvicorn.run(...)` plus startup code — strong candidate for `# pragma: no cover  # entry point`

Most other missing lines are scattered single-line error branches in endpoint handlers (400/403/404/500 returns, Redis unavailable, invalid inputs).

## Triage guidance

- **Lines 1872-1888**: if `if __name__ == "__main__":` uvicorn bootstrap, add `# pragma: no cover  # module entry point not invoked in tests` with comment explanation
- **Lines 1844-1853**: check context — may be startup handler or similar; test if possible, pragma if truly startup-only
- All other lines: test via `TestClient` with crafted requests that trigger error branches

## Testing approach

FastAPI `TestClient` makes endpoint testing straightforward:
```python
from fastapi.testclient import TestClient
client = TestClient(app)
response = client.post("/repos/nonexistent/start", json={...})
assert response.status_code == 404
```

For Redis/state mocking, use existing fixture patterns in `tests/test_web_app.py`.

## Tests to add

Split across existing test files by topic:
- Upload endpoint edge cases → `tests/test_upload.py`
- Settings edge cases → `tests/test_settings.py`
- Alerts edge cases → `tests/test_alerts.py`
- Observability → `tests/test_observability.py`
- Remaining → `tests/test_web_app.py`

Per missing line, identify endpoint and construct request that triggers the branch.

## Restrictions

- Do NOT modify `src/web/app.py` except dead code / pragma with inline comments
- Do NOT add new endpoints or change response formats
- Use `TestClient`, do NOT start real uvicorn server in tests
- Use fake Redis (`fakeredis` or monkeypatched dict)

## Success criteria

- `pytest tests/test_web_app.py tests/test_settings.py tests/test_upload.py tests/test_observability.py tests/test_alerts.py --cov=src/web/app --cov-report=term-missing --cov-fail-under=100` exits 0
- 100% coverage for `src/web/app.py`
- All existing tests pass
- `ruff check src tests` exits 0
- `scripts/ci.sh` exits 0
- Any pragma added on lines 1872-1888 has inline comment "entry point" or equivalent justification
