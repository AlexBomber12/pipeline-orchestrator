# PR-117: 100% coverage for src/daemon/handlers/breach.py

Branch: pr-117-coverage-handler-breach
- Type: refactor
- Complexity: low
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/daemon/handlers/breach.py` is at 85% line coverage with 10 missing lines. Handler deals with SLA/budget breach detection.

Missing lines: `42-44`, `49`, `84-85`, `87`, `90`, `107-108`.

Many small scattered branches. Pattern suggests edge-case threshold checks.

## Scope

Bring `src/daemon/handlers/breach.py` to 100% line coverage by adding tests in `tests/test_breach_handler.py` (create if needed).

## Missing branches

- Lines 42-44 (3 lines): early threshold branch
- Line 49: continuation branch
- Lines 84-85 (2 lines): paired logic
- Line 87: single branch
- Line 90: single branch
- Lines 107-108 (2 lines): final pair

## Triage

Read source, classify each. Breach logic is typically threshold comparisons → most branches are real and testable with controlled state inputs.

## Testing approach

Construct RepoState objects with specific values that trigger each missing branch. Use existing fixture patterns.

## Tests to add

- Per missing branch, one targeted test with clear scenario name

## Restrictions

Standard: no source modifications except dead/pragma.

## Success criteria

- `pytest tests/test_breach_handler.py --cov=src/daemon/handlers/breach --cov-report=term-missing --cov-fail-under=100` exits 0
- 100% coverage for `src/daemon/handlers/breach.py`
- All existing tests pass
- `ruff check src tests` exits 0
- `scripts/ci.sh` exits 0
