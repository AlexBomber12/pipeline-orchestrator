---
status: TODO
---

# PR-400: Verify the complete AUD-01 contract and document its deployment boundary

Branch: pr-400-trusted-cycle-contracts
- Type: bugfix
- Complexity: high
- Depends on: PR-399
- Priority: 1
- Coder: any

Audit references: A08, A09, A10, A11, A12, A13, A14, A35, A36.

## Problem and intended result

Unit coverage can remain green while a real call chain loses completeness, accepts stale review or skips process preservation. Existing testbed settings allow merge without checks/review, so that testbed's success alone does not establish the production gate. Verify the integrated contracts implemented in this sprint through real internal entry points and controlled external boundaries.

This is an integration acceptance task for the preceding implementation, not a new architecture or a general cleanup sprint. Add targeted regressions and fix only concrete integration defects within the AUD-01 scope if revealed.

## Read before editing

- The current merged GitHub evidence, review-request, merge, PR reconciliation, supervisor and preservation implementations from the prerequisite chain.
- tests/runner/, tests/github/, tests/e2e/conftest.py, tests/e2e/lib/coder_shim.py and docs/local-e2e.md.
- scripts/ci.sh, config.test.yml, requirements-test.txt and .github/workflows/ci.yml to understand existing test boundaries without changing their security policy.
- docs/operations.md and docs/architecture-state-machine.md for the sections affected by this sprint.

## Required behavior

1. Add a compact scenario suite driving real WATCH/MERGE/recovery methods and concrete evidence objects. Mock only GitHub transport and coder execution where isolation requires it; do not preassign APPROVED/SUCCESS to bypass the code under test.
   Set auto_merge to true in the scenario policy.
   Set no-checks/no-review exemptions to false. Require unit and integration contexts.
2. Cover valid automatic merge, partial CI, untrusted +1, stale anchor after HEAD change, a pre-merge sync commit, a HEAD race at merge, active PR missing from discovery and restart of an active proven request. Assert merge invocation/absence, retained task/run ownership and persisted evidence, not only final enum values.
3. Cover process cleanup followed by work preservation and a destructive transition with isolated Git and subprocess fixtures. Verify ordering and recovery bytes. Exercise a cleanup/preservation failure so the destructive operation is not called. Test fixtures must always remove only their own processes and temporary paths.
4. Where a real Redis boundary is required, use the existing integration environment and its testbed isolation. Keep normal unit tests independent of an installed gh binary and production Redis. AUTO PR cannot run Docker locally; integration execution remains with the existing GHA job. Document separately which checks ran locally and which ran in GHA.
5. Preserve the #529/#530 historical recovery fixtures and the completion manifest contract. New sprint tasks are genuine new TODO work; do not use the earlier 489-DONE total as the expected total after this sprint is admitted. Verify no historical task regresses and do not rewrite historical files to make a counter match.
6. Update the existing operations/state-machine sections for only the delivered merge, supervision and preservation behavior, marking contradictory old text as superseded. Record residual scope: Retry/Reset command consistency, global Stop durability, AWAY/presence/5-60, cross-process ownership and stronger sandboxing are not closed by this sprint.
7. Document a deployment handoff identifying source SHA, required_checks adoption, reviewer identity policy, state-format compatibility, recovery snapshot location and rollback compatibility. Starting the installed version is an operator operation outside AUTO PR. Do not pull code into /app, restart containers, edit real config or run failure drills against SERVER.

## Acceptance criteria

- Every scenario reaches the real decision boundary; none fabricates the decision it is intended to test.
- The controlled happy path merges automatically with valid current-HEAD evidence.
- Each invalid/unknown evidence case prevents merge and remains recoverable without duplicate dispatch.
- Process-tree cleanup and snapshot verification precede destructive work; their failure blocks it.
- Local CI is green; the existing GHA integration job passes; new scenarios are actually collected and run in their intended gate.
- Docs distinguish source merged from version deployed, explain required settings adoption and list remaining sprint-2 issues honestly.

## Files and boundaries

Expected changes: relevant existing test modules, narrowly scoped regression fixtures and existing operations/state-machine documentation. A new tests module under the established test hierarchy is allowed for the integrated scenarios. Do not create a second test framework, alter GitHub workflow permissions, disable testbed gates or expand to UI redesign and all audit findings.

Success means AUD-01 has end-to-end evidence at its internal boundaries and a concrete deployment handoff, rather than relying solely on aggregate line coverage.

## Execution and scope

Repository: AlexBomber12/pipeline-orchestrator. Sprint: AUD-01, Trusted PR execution. Execution mode chosen by the owner: Orchestrator AUTO PR. The daemon supplies this complete task inline with explicit Task and File headers. Use the supplied task and Branch; do not discover work through tasks/QUEUE.md or read other task specs. Prerequisites are represented by Depends on and their merged implementation.

Planning baseline is ae3c141812c7f3371895d4575b29f1374fe5c4bc. Implement on the current configured base after prerequisite tasks have merged, retaining their changes. Preserve automatic coding, fixing and merge. Preserve PR #529/#530 historical parsing and completion evidence; do not rewrite historical task files, the completion manifest, or task statuses for this implementation.

Work in the daemon-provided managed clone. This task does not deploy the application, restart services, edit the installed /app checkout, read production credentials, or change GitHub repository settings. Use isolated fixtures for failure cases. Do not send test mutations to working repositories. An unmet prerequisite or inability to implement the scoped contract must produce an explicit ESCALATE result, not weakened checks.

## Required delivery gate

Run bash scripts/ci.sh to exit 0 with the repository's lint and coverage requirements. Extend the existing relevant test modules where practical. Generate artifacts/ci.log, artifacts/pr.patch and artifacts/structure.txt for review without committing those artifacts. GitHub Actions unit and integration jobs must be green, and Codex review must have a valid current-change +1. The existing GHA integration job owns the Docker testbed; an AUTO PR coder must not attempt Docker operations inside its container. Keep the review trigger comment exactly @codex review. Never disable checks or reduce coverage to obtain green results.

In the PR description state the changed behavior, actual verification results, audit IDs, migrated call sites, compatibility limits and deployment requirements. A passing mock alone is not evidence of a cross-component contract. Do not claim production validation from fixture results.
