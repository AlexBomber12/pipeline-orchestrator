# PR-107a: 100% coverage for merge.handle_merge

Branch: pr-107a-coverage-merge-handle
- Type: refactor
- Complexity: medium
- Depends on: none
- Priority: 3
- Coder: any

## Problem

First of three sub-PRs to bring `src/daemon/handlers/merge.py` (60% → 100%). This sub-PR covers the `handle_merge()` method at lines 32-173.

Missing lines in scope: `35-36`, `67-72`, `102-109`, `139-145`. Total ~20 missing lines across 4 blocks.

## Scope

Bring the `handle_merge()` function to full coverage of its missing branches. Do NOT touch `_mark_queue_done` (covered by PR-107b) or `_resolve_pending_queue_sync`/`_escalate_queue_sync_if_expired` (covered by PR-107c).

Add tests in `tests/test_runner.py` (handler tests live there per project convention, not in dedicated test files).

## Missing blocks

- Lines 35-36: early-exit guard at top of `handle_merge`
- Lines 67-72: 6-line block, pre-merge sync failure handling
- Lines 102-109: 8-line block, conflict detection and branch setup for resolve path
- Lines 139-145: 7-line block, Claude CLI invocation for conflict resolution

## Triage

Read source, classify each block:
1. Write test (primary; all 4 blocks are real conflict-scenarios paths)
2. Delete if truly unreachable (rare)
3. Pragma with rationale (reserve for platform-specific or signal-handler edges)

## Testing approach

Mock via `monkeypatch`:
- `git_ops._git` for specific git command outcomes (conflict vs clean merge)
- `github_client.get_pr_status` / `is_pr_merged` for PR state
- `claude_cli.run_claude_async` for conflict-resolution invocation
- `MetricsStore.save` for RunRecord write verification

Use fresh `RepoState` per test to isolate state.

## Tests to add in `tests/test_runner.py`

Indicative names — adjust to actual code:

- `test_handle_merge_early_exit_when_<precondition>` for lines 35-36
- `test_handle_merge_pre_sync_failure_logs_and_returns` for 67-72
- `test_handle_merge_detects_conflict_and_prepares_branch` for 102-109
- `test_handle_merge_invokes_claude_for_conflict_resolution` for 139-145

## Success criteria

- `pytest tests/test_runner.py --cov=src/daemon/handlers/merge --cov-report=term-missing` runs with exit 0
- The specific line numbers 35, 36, 67-72, 102-109, 139-145 no longer appear in the "Missing" column of the coverage report for `src/daemon/handlers/merge.py`
- Other currently-missing lines in merge.py (174-353) MAY still appear in Missing — those are scope of PR-107b and PR-107c
- All existing tests pass
- `ruff check src tests` exits 0
- `scripts/ci.sh` exits 0
- No diff in `src/daemon/handlers/merge.py` (test-only change; no dead code removal in this sub-PR without clear inline justification)

## Restrictions

- Do NOT modify `src/daemon/handlers/merge.py` except for deletion of truly dead code within lines 35-145 with inline comment explaining why
- Do NOT touch lines 174+ in merge.py (other sub-PRs own those)
- Do NOT modify any other source module
- Do NOT change merge semantics, retry counts, or conflict-resolution prompts
- Do NOT add new features even if obvious gaps appear
