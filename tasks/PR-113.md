# PR-113: 100% coverage for src/task_status.py

Branch: pr-113-coverage-task-status
- Type: refactor
- Complexity: low
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/task_status.py` is at 84% line coverage with 22 missing lines. Module derives task status from git state (PR-085).

Missing lines: `63`, `84-88`, `98`, `120`, `130`, `132`, `141`, `193-196`, `205`, `261`, `285`, `300-301`, `304`, `308`.

## Scope

Bring `src/task_status.py` to 100% line coverage by extending `tests/test_task_status.py`.

## Missing blocks

Scattered small branches, typical pattern of uncovered edge cases:
- Lines 84-88 (5 lines): probably one git-state parsing fallback
- Lines 193-196 (4 lines): probably branch-not-found or stale-branch handling
- Lines 300-308 (scattered): probably status-transition edge cases

## Triage

All likely test-addable. Task status derivation is deterministic logic with no side effects; easy to test.

## Testing approach

Use `monkeypatch` of `github_client` and `git_ops._git` for controlled inputs. Already established pattern in existing `tests/test_task_status.py`.

## Tests to add

Targeted tests for each missing line/block. Status derivation has a finite set of outcomes (DONE, IN_FLIGHT, FAILED, NEW, etc.) — enumerate untested transitions.

## Restrictions

Standard: no source modifications except dead/pragma.

## Success criteria

- `pytest tests/test_task_status.py --cov=src/task_status --cov-report=term-missing --cov-fail-under=100` exits 0
- 100% coverage for `src/task_status.py`
- All existing tests pass
- `ruff check src tests` exits 0
- `scripts/ci.sh` exits 0
