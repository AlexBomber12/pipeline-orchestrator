# PR-099: 100% coverage for src/coders/codex.py

Branch: pr-099-coverage-coders-codex
- Type: refactor
- Complexity: low
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/coders/codex.py` is at 88% line coverage with 9 missing lines: `45-48`, `123-124`, `139`, `143`, `165`.

## Scope

Bring `src/coders/codex.py` to 100% line coverage by adding targeted tests in `tests/test_codex_plugin.py`.

## Missing lines at time of task creation

- Lines 45-48: `_run_auth_command` exception branches: `PermissionError` (45-46) and `TimeoutExpired` (47-48)
- Lines 123-124: `check_auth()` fallback — when `version` stderr is not recognized as "not installed", construct detail from `version_line or fallback`
- Line 139: `check_auth()` branch — when `codex login status` fails with "not found" in output
- Line 143: `check_auth()` branch — when `OPENAI_API_KEY` is set but unverified, detail string is augmented
- Line 165: `rate_limit_patterns()` return with both patterns

All are real paths. Must be tested.

## Triage

All lines: write tests. None should be deleted or pragma'd.

## Tests to add in `tests/test_codex_plugin.py`

- `test_auth_command_returns_126_on_permission_error`: monkeypatch `subprocess.run` to raise `PermissionError`, assert return is `(126, "", <error text>)`.
- `test_auth_command_returns_124_on_timeout`: monkeypatch to raise `subprocess.TimeoutExpired`, assert return is `(124, "", <timeout text>)`.
- `test_check_auth_detail_fallback_for_unknown_version_stderr`: mock `_run_auth_command` to return non-zero on `codex --version` with stderr that doesn't match "not found" or "no such file", assert `check_auth()` returns `{"status": "error", "detail": <stderr or fallback>}`.
- `test_check_auth_login_status_not_found_returns_cli_not_installed`: mock the login-status probe to return non-zero with "not found" in output, assert `check_auth()` detail is `"codex CLI not installed"`.
- `test_check_auth_detail_mentions_api_key_when_set_but_unverified`: mock login-status probe to fail, set `env["OPENAI_API_KEY"]=...`, assert detail contains `"OPENAI_API_KEY set but unverified"`.
- `test_rate_limit_patterns_returns_both_codex_patterns`: call `CodexPlugin().rate_limit_patterns()`, assert list has length 2 and contains both `_CODEX_RETRY_PATTERN` and `_CODEX_USAGE_LIMIT_PATTERN`.

## Restrictions

- Do NOT modify `src/coders/codex.py`.
- Do NOT modify any other source module.
- Use existing test fixtures in `tests/test_codex_plugin.py`.

## Success criteria

- `pytest tests/test_codex_plugin.py --cov=src/coders/codex --cov-report=term-missing --cov-fail-under=100` exits 0.
- Overall coverage for `src/coders/codex.py` is 100%.
- All existing tests still pass.
- `ruff check src tests` exits 0.
- `scripts/ci.sh` exits 0.
- No diff in `src/coders/codex.py`.
