# PR-245: MCP read-only tools (get_task_schema + get_agents_md_template)

Branch: pr-245-mcp-readonly-tools
- Type: feature
- Complexity: low
- Depends on: PR-244
- Priority: 2
- Coder: claude

## Problem

The MCP server scaffold from PR-243 exposes only a healthcheck tool. v1 needs four production tools per the MCP architectural decision in `docs/roadmap.md`. This PR ships the two read-only tools that wrap existing orchestrator content with no logic changes:

1. `get_task_schema()` returns the canonical task file format definition. Today, LLM coders generating `tasks/PR-*.md` specs read `docs/TASK_SCHEMA.md` indirectly (via being scaffolded into managed repos or via prompt context). After this PR, any MCP-aware client can pull the schema fresh on demand: when the schema evolves in the orchestrator repo, all clients see the new version on next call without re-scaffolding.

2. `get_agents_md_template(repo_slug)` returns the daemon-managed AGENTS.md sections for a given target repo. Today, this content is computed by `src.onboarding.agents_md_template.daemon_managed_content(repo_slug)` and copied into managed repos by the scaffolder. After this PR, MCP clients can fetch it on demand for review or onboarding-time conflict scanning (Sprint 14 work).

Both tools are pure functions: read filesystem or call existing module function, return string. No side effects, no Redis, no GitHub API.

## Scope

Two deliverables:

1. New module `src/mcp/tools/readonly.py` defining the two functions and registering them with the MCP server via `@mcp.tool()` decorator.
2. Wire `src/mcp/server.py` to import the readonly tools module so registrations fire at startup.

Existing `docs/TASK_SCHEMA.md` and `src.onboarding.agents_md_template` are wrapped, not modified. If an LLM client misuses the returned schema (e.g., generates a non-conformant task spec), validation happens server-side at upload time per existing `parse_task_header` + `validate_queue` machinery; v1 accepts that mistakes are caught downstream.

## Files to modify

`src/mcp/tools/__init__.py` (new):

```
"""MCP tool implementations.

Tools register themselves with the FastMCP server at module import time
via the ``@mcp.tool()`` decorator. The server entrypoint
(``src.mcp.server``) imports each tool module so registrations fire
before ``mcp.run`` is called.
"""
```

`src/mcp/tools/readonly.py` (new):

```
"""Read-only MCP tools.

Wraps existing orchestrator content with no logic changes. Both tools
are pure: filesystem read or module function call, return string.
"""

from __future__ import annotations

import logging
from pathlib import Path

from src.mcp.server import mcp
from src.onboarding.agents_md_template import daemon_managed_content

logger = logging.getLogger(__name__)

# Path to TASK_SCHEMA.md inside the container. Image mounts the
# orchestrator repo at ``/app`` (read-only per docker-compose.yml).
_TASK_SCHEMA_PATH = Path("/app/docs/TASK_SCHEMA.md")


@mcp.tool()
def get_task_schema() -> str:
    """Return the canonical task file format definition.

    Reads ``docs/TASK_SCHEMA.md`` from the orchestrator repo. LLM
    clients generating ``tasks/PR-*.md`` specs should call this tool
    for the authoritative format including type vocabulary, complexity
    levels, depends-on syntax, and synonym handling rules.

    Returns:
        Markdown content of ``docs/TASK_SCHEMA.md``.

    Raises:
        FileNotFoundError: if the schema file is missing from the
            mounted repo. This indicates a deployment misconfiguration
            (e.g., volume mount missing or repo not present).
    """
    if not _TASK_SCHEMA_PATH.exists():
        raise FileNotFoundError(
            f"TASK_SCHEMA.md not found at {_TASK_SCHEMA_PATH}. "
            "Verify the docker-compose mount of the orchestrator repo."
        )
    return _TASK_SCHEMA_PATH.read_text(encoding="utf-8")


@mcp.tool()
def get_agents_md_template(repo_slug: str) -> str:
    """Return the daemon-managed AGENTS.md sections for a target repo.

    Wraps ``src.onboarding.agents_md_template.daemon_managed_content``.
    LLM clients reviewing or scaffolding a target repo can fetch the
    canonical orchestrator-managed sections without depending on a
    local copy in that repo.

    Args:
        repo_slug: Target repo slug in ``owner__repo`` form (e.g.
            ``AlexBomber12__pipeline-orchestrator``,
            ``AlexBomber12__megaraid-dashboard``).

    Returns:
        Markdown content with HTML markers (``<!-- BEGIN: ... -->``
        and ``<!-- END: ... -->``) wrapping each managed section.
    """
    return daemon_managed_content(repo_slug)
```

`src/mcp/server.py` — add tool module imports near the bottom of the file, after the `mcp` instance is defined and before `main()`:

Find:

```
@mcp.tool()
def healthcheck() -> dict[str, str]:
    """Return server liveness and version info.
    ...
    return {"status": "ok", "service": "pipeline-orchestrator-mcp", "version": "v1"}


def main() -> None:
```

Replace with:

```
@mcp.tool()
def healthcheck() -> dict[str, str]:
    """Return server liveness and version info.
    ...
    return {"status": "ok", "service": "pipeline-orchestrator-mcp", "version": "v1"}


# Tool module imports MUST happen after the ``mcp`` instance is created
# so the ``@mcp.tool()`` decorators can register against it. Keep these
# imports at module level so registration fires at server startup.
from src.mcp.tools import readonly  # noqa: E402, F401


def main() -> None:
```

The `noqa: E402` suppresses the "module level import not at top" lint complaint, which is unavoidable here because the import order is semantically required: `mcp` must exist before tool modules import it. The `F401` suppresses unused-import; the import IS the registration.

## Tests

`tests/mcp/test_readonly_tools.py` (new):

```
"""Tests for read-only MCP tools."""

from __future__ import annotations

from pathlib import Path
from unittest.mock import patch

import pytest


def test_get_task_schema_returns_markdown_content():
    """get_task_schema reads TASK_SCHEMA.md and returns its content."""
    from src.mcp.tools.readonly import get_task_schema

    # In test environment the path may differ; patch to point at
    # the actual file in the repo.
    real_path = Path(__file__).resolve().parents[2] / "docs" / "TASK_SCHEMA.md"
    with patch("src.mcp.tools.readonly._TASK_SCHEMA_PATH", real_path):
        content = get_task_schema()

    assert content.startswith("# Task file schema")
    assert "Branch:" in content
    assert "Type:" in content
    # Verify synonym map docs are present (key part of schema).
    assert "synonym" in content.lower()


def test_get_task_schema_raises_when_file_missing(tmp_path):
    """get_task_schema raises FileNotFoundError when schema is missing."""
    from src.mcp.tools import readonly

    missing_path = tmp_path / "does-not-exist.md"
    with patch.object(readonly, "_TASK_SCHEMA_PATH", missing_path):
        with pytest.raises(FileNotFoundError, match="TASK_SCHEMA.md not found"):
            readonly.get_task_schema()


def test_get_agents_md_template_returns_managed_content_for_orchestrator():
    """get_agents_md_template wraps daemon_managed_content correctly."""
    from src.mcp.tools.readonly import get_agents_md_template

    content = get_agents_md_template("AlexBomber12__pipeline-orchestrator")

    # Output must contain HTML markers that wrap managed sections;
    # exact section names match what scaffolder emits.
    assert "<!-- pipeline-orchestrator: managed BEGIN" in content
    assert "<!-- pipeline-orchestrator: managed END" in content


def test_get_agents_md_template_handles_arbitrary_slug():
    """get_agents_md_template works for any repo slug, not just orchestrator self."""
    from src.mcp.tools.readonly import get_agents_md_template

    content = get_agents_md_template("AlexBomber12__megaraid-dashboard")
    assert "<!-- pipeline-orchestrator: managed BEGIN" in content


def test_readonly_tools_registered_with_mcp_server():
    """Both tools appear in the MCP server's tool registry after import."""
    import asyncio

    from src.mcp.server import mcp

    # Importing the readonly module triggers @mcp.tool() registration.
    from src.mcp.tools import readonly  # noqa: F401

    tools = asyncio.run(mcp.list_tools())
    tool_names = {t.name for t in tools}
    assert "get_task_schema" in tool_names
    assert "get_agents_md_template" in tool_names
```

The `_TASK_SCHEMA_PATH` constant is patched at test time because the absolute container path `/app/docs/TASK_SCHEMA.md` does not exist on the GHA runner; tests resolve the actual file via relative path from the test file location.

## Files NOT to touch

- `src/onboarding/agents_md_template.py` — wrapped, not modified. The function signature `daemon_managed_content(repo_slug)` already returns the markdown content; no API changes needed.
- `docs/TASK_SCHEMA.md` — read as-is, no edits.
- `src/queue_parser.py` — unrelated to read-only tools (covered by PR-245).
- `Dockerfile`, `requirements.txt` — no new deps; `mcp[cli]` from PR-243 covers all.

## Implementation notes

The two tools deliberately do NOT cache results in v1. `docs/TASK_SCHEMA.md` is read on every call; `daemon_managed_content` is called fresh each time. Caching can be added in v2 if measurements show this matters. For now, simplicity wins: schema files are small (a few KB) and read latency is sub-millisecond on a mounted volume.

`get_agents_md_template` does not validate `repo_slug` shape. If a malformed slug is passed, `daemon_managed_content` is responsible for handling it (raise or return empty content per its existing contract). v1 trusts callers.

After this PR ships, verify end-to-end inside the daemon host:

```
docker compose restart mcp
curl -s http://localhost:5173/...  # FastMCP exposes a tool-list endpoint
# Or invoke the tool via a test MCP client:
python -c "
from mcp import ClientSession
import asyncio
# ...
"
```

The exact client invocation depends on the `mcp[cli]` SDK's client library; refer to the SDK examples in the installed package or the modelcontextprotocol.io documentation for the current pattern.
