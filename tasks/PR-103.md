# PR-103: 100% coverage for src/daemon/handlers/hung.py

Branch: pr-103-coverage-handler-hung
- Type: refactor
- Complexity: low
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/daemon/handlers/hung.py` is at 86% line coverage with 12 missing lines: `31-32`, `133-134`, `153-157`, `175-179`. Handler implements the HUNG state transition and hung-PR recovery paths.

## Scope

Bring `src/daemon/handlers/hung.py` to 100% line coverage by adding targeted tests in `tests/test_hung_handler.py` (create if needed) or the existing sibling test file.

## Missing lines at time of task creation

Investigate the following blocks in the source and classify each:
- Lines 31-32: Early-exit guard at top of `handle_hung()`, likely a state precondition check
- Lines 133-134: Mid-handler branch, likely a condition on state.current_pr or state.current_task
- Lines 153-157: 5-line block, likely error-handling or fallback branch during PR resolution
- Lines 175-179: 5-line block, likely similar to 153-157, paired recovery path

## Triage

For each block, look at the source and decide:
1. Write a test (preferred — these are HUNG state transitions which are real)
2. Delete as dead (only if truly unreachable)
3. Pragma (only for legitimate edge cases — unlikely here)

## Tests to add

Examples — adjust to actual code:
- `test_handle_hung_early_exit_when_<precondition>`: state with condition that matches lines 31-32 guard.
- `test_handle_hung_takes_<branch>_when_<condition>`: state exercising lines 133-134 branch.
- `test_handle_hung_recovery_path_when_<error>`: trigger the 153-157 block via monkeypatched dependency.
- `test_handle_hung_fallback_path_when_<other_error>`: trigger 175-179 block similarly.

## Restrictions

- Do NOT modify `src/daemon/handlers/hung.py`.
- Do NOT modify any other source module.
- Do NOT change HUNG state detection thresholds.

## Success criteria

- `pytest tests/test_hung_handler.py --cov=src/daemon/handlers/hung --cov-report=term-missing --cov-fail-under=100` exits 0.
- Overall coverage for `src/daemon/handlers/hung.py` is 100%.
- All existing tests still pass.
- `ruff check src tests` exits 0.
- `scripts/ci.sh` exits 0.
- No diff in `src/daemon/handlers/hung.py`.
