# PR-067: Test hygiene - ruff and pytest-asyncio warnings

Branch: pr-067-test-hygiene
- Type: refactor
- Complexity: low
- Depends on: none
- Priority: 5
- Coder: any

## Problem

38 ruff warnings in tests/ (unused imports, formatting). pytest-asyncio deprecation warning about asyncio_default_fixture_loop_scope not set.

## Scope

Fix all ruff warnings in tests/. Add `asyncio_mode = "auto"` to pyproject.toml [tool.pytest.ini_options]. No production code changes.

## Changes

### tests/*.py
Fix ruff warnings: remove unused imports, fix formatting issues. Run `ruff check tests/ --fix` for auto-fixable, manual fix for the rest.

### pyproject.toml
Add under [tool.pytest.ini_options]:
```
asyncio_mode = "auto"
```

## Success criteria
- `ruff check tests/` exits 0 with no warnings
- `ruff check src/` still exits 0
- `pytest` runs without asyncio deprecation warning
- scripts/ci.sh exits 0
- All existing tests pass
