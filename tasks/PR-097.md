# PR-097: 100% coverage for src/coders/claude.py

Branch: pr-097-coverage-coders-claude
- Type: refactor
- Complexity: low
- Depends on: none
- Priority: 3
- Coder: any

## Problem

`src/coders/claude.py` is at 94% line coverage with 3 missing lines: `39`, `95`, `115`. Existing tests in `tests/test_claude_plugin.py` cover the happy path; these three lines represent auth edge cases and default-path fallbacks.

## Scope

Bring `src/coders/claude.py` to 100% line coverage by adding targeted tests in `tests/test_claude_plugin.py`.

## Missing lines at time of task creation

- Line 39: `return 126, "", str(exc)` inside `_run_auth_check()` when the subprocess raises `PermissionError`
- Line 95: `cfg = load_config(kwargs.pop("config_path", CONFIG_PATH))` inside `create_usage_provider()` when called without an explicit `config` kwarg (default path fallback)
- Line 115: `return [_ANTHROPIC_RATE_LIMIT_PATTERN]` inside `rate_limit_patterns()`

All three are real paths. Must be tested.

## Triage

- Line 39: write a test with monkeypatched `subprocess.run` raising `PermissionError`
- Line 95: write a test invoking `create_usage_provider()` without `config` kwarg, verify `load_config` is called with expected path
- Line 115: write a simple test invoking `rate_limit_patterns()` and asserting returned list contains `_ANTHROPIC_RATE_LIMIT_PATTERN`

## Tests to add in `tests/test_claude_plugin.py`

- `test_auth_check_returns_126_on_permission_error`: monkeypatch `subprocess.run` to raise `PermissionError("denied")`, call the auth-check function (or whichever public method invokes `_run_auth_check`), assert return code is 126 and stderr contains the permission error text.
- `test_create_usage_provider_loads_config_from_default_path`: call `ClaudePlugin().create_usage_provider()` with no `config` kwarg, monkeypatch `load_config` to return a minimal `AppConfig`, assert `load_config` was called.
- `test_rate_limit_patterns_returns_anthropic_pattern`: call `ClaudePlugin().rate_limit_patterns()`, assert returned list has length 1 and contains the Anthropic rate-limit regex.

## Restrictions

- Do NOT modify `src/coders/claude.py`.
- Do NOT modify any other source module.
- Use existing test fixtures where possible.

## Success criteria

- `pytest tests/test_claude_plugin.py --cov=src/coders/claude --cov-report=term-missing --cov-fail-under=100` exits 0.
- Overall coverage for `src/coders/claude.py` is 100%.
- All existing tests still pass.
- `ruff check src tests` exits 0.
- `scripts/ci.sh` exits 0.
- No diff in `src/coders/claude.py`.
