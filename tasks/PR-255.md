# PR-255: Cancellation policy v1 - SignalSource Protocol + Manual/Heartbeat/ActiveHours sources

Branch: pr-255-cancellation-signal-sources
- Type: feature
- Complexity: medium
- Depends on: PR-254
- Priority: 2
- Coder: claude

## Problem

Cancellation policy v1 from `docs/roadmap.md`. Daemon currently has no first-class concept of "is the operator available right now". Long-running tasks consume API budget overnight without operator awareness; off-hours work on speculative coder iterations gets billed regardless of whether anyone will look at results before noon. Manual pause is too coarse — operator forgets, or pauses everything when only some repos need pausing.

This PR introduces a SignalSource Protocol and ships three concrete sources that compose into "is operator available":

1. **ManualOverrideSource** — operator clicks Available/Away in dashboard. 3-state: AVAILABLE, AWAY, AUTO (defer to other sources).
2. **HeartbeatSource** — dashboard visit pings a Redis heartbeat key with TTL. Recent ping = available.
3. **ActiveHoursSource** — config-tunable hours-of-day window per timezone. Outside window = away.

Composition policy: AWAY if ManualOverride==AWAY, else AVAILABLE if any source says AVAILABLE, else AWAY. Failure-safe: any source raising exception is treated as AVAILABLE (do not block coder work on observability subsystem failures).

This PR ships only the sources and protocol. PR-256 ships the UI chip; PR-257 ships dependency-aware blocked_set.

## Scope

Three deliverables:

1. New module `src/cancellation/availability.py` with `SignalSource` Protocol and three implementations.
2. Composition function `is_operator_available(redis_client, config) -> AvailabilityState`.
3. Tests covering each source individually + composition + failure-safe fallback.

## Files to modify

New file `src/cancellation/availability.py`:

```
"""Operator availability signal sources - Cancellation policy v1.

Protocol-driven design lets future sources (calendar, Slack presence,
phone DnD) plug in without changing composition logic. Three v1
sources cover the common cases. Failure-safe by design: any source
exception treated as AVAILABLE.

PR-255.
"""

from __future__ import annotations

from dataclasses import dataclass
from datetime import datetime, time, timezone, timedelta
from enum import Enum
from typing import Any, Protocol
from zoneinfo import ZoneInfo


class AvailabilityState(str, Enum):
    AVAILABLE = "AVAILABLE"
    AWAY = "AWAY"


class SignalSource(Protocol):
    """Source of an availability signal."""

    name: str

    async def query(self) -> AvailabilityState | None:
        """Return signal or None to defer.

        None = "I have no opinion, defer to other sources".
        AVAILABLE = "I am sure operator is available".
        AWAY = "I am sure operator is unavailable".
        """
        ...


@dataclass
class ManualOverrideSource:
    """Operator-set 3-state override.

    Reads ``operator_override`` Redis key:
    - "AVAILABLE" -> AvailabilityState.AVAILABLE
    - "AWAY" -> AvailabilityState.AWAY
    - "AUTO" or missing -> None (defer)
    """

    name: str = "manual_override"
    redis_client: Any = None

    async def query(self) -> AvailabilityState | None:
        try:
            raw = await self.redis_client.get("operator_override")
        except Exception:
            return None
        if raw is None:
            return None
        if isinstance(raw, bytes):
            raw = raw.decode("utf-8")
        if raw == "AVAILABLE":
            return AvailabilityState.AVAILABLE
        if raw == "AWAY":
            return AvailabilityState.AWAY
        return None


@dataclass
class HeartbeatSource:
    """Dashboard visit heartbeat with TTL.

    Reads ``operator_heartbeat`` Redis key (set by dashboard middleware
    on each authenticated request, TTL N minutes). Present = AVAILABLE,
    absent = None (defer rather than declaring AWAY, since absence
    could be legitimate AVAILABLE-but-not-clicking).
    """

    name: str = "heartbeat"
    redis_client: Any = None

    async def query(self) -> AvailabilityState | None:
        try:
            present = await self.redis_client.exists("operator_heartbeat")
        except Exception:
            return None
        return AvailabilityState.AVAILABLE if present else None


@dataclass
class ActiveHoursSource:
    """Config-tunable active hours window per timezone."""

    name: str = "active_hours"
    start_hour: int = 9
    end_hour: int = 21
    timezone_name: str = "Europe/Rome"

    async def query(self) -> AvailabilityState | None:
        try:
            tz = ZoneInfo(self.timezone_name)
            now = datetime.now(tz=tz)
            if self.start_hour <= now.hour < self.end_hour:
                return AvailabilityState.AVAILABLE
            return AvailabilityState.AWAY
        except Exception:
            return None


async def is_operator_available(
    sources: list[SignalSource],
) -> AvailabilityState:
    """Compose multiple signal sources into a single availability verdict.

    Policy:
    - ManualOverrideSource takes precedence over all others (operator
      explicit will).
    - Among the rest, AVAILABLE wins if any source says AVAILABLE.
    - Otherwise AWAY if any source says AWAY.
    - Default AVAILABLE (failure-safe).

    PR-255.
    """
    manual_verdict: AvailabilityState | None = None
    other_verdicts: list[AvailabilityState] = []

    for source in sources:
        try:
            verdict = await source.query()
        except Exception:
            verdict = None
        if verdict is None:
            continue
        if source.name == "manual_override":
            manual_verdict = verdict
        else:
            other_verdicts.append(verdict)

    if manual_verdict is not None:
        return manual_verdict
    if AvailabilityState.AVAILABLE in other_verdicts:
        return AvailabilityState.AVAILABLE
    if AvailabilityState.AWAY in other_verdicts:
        return AvailabilityState.AWAY
    return AvailabilityState.AVAILABLE
```

`src/web/middleware.py` (or wherever request middleware lives) — add heartbeat ping on each authenticated request:

```
@app.middleware("http")
async def operator_heartbeat_middleware(request: Request, call_next):
    response = await call_next(request)
    redis_client = getattr(request.app.state, "redis", None)
    if redis_client is not None and request.url.path.startswith("/"):
        try:
            await redis_client.set(
                "operator_heartbeat", "1", ex=300  # 5 min TTL
            )
        except Exception:
            pass  # Failure-safe.
    return response
```

`src/orchestrator_config.py` — extend with active hours config:

```
@dataclass
class DaemonConfig:
    ...
    operator_active_hours_start: int = 9
    operator_active_hours_end: int = 21
    operator_timezone: str = "Europe/Rome"
```

`config.yml` and `config.production.example.yml` — add the fields with defaults.

## Tests

`tests/test_availability_sources.py` (new):

- `test_manual_override_available`: Redis returns "AVAILABLE", source returns AVAILABLE.
- `test_manual_override_away`: returns AWAY.
- `test_manual_override_auto_defers`: returns None.
- `test_manual_override_redis_failure_returns_none`.
- `test_heartbeat_present_returns_available`.
- `test_heartbeat_absent_returns_none`.
- `test_active_hours_within_window`: time inside 9-21, AVAILABLE.
- `test_active_hours_outside_window`: time outside, AWAY.
- `test_composition_manual_overrides_others`: manual=AWAY, heartbeat=AVAILABLE, result=AWAY.
- `test_composition_available_wins_among_others`: heartbeat=None, active_hours=AVAILABLE, result=AVAILABLE.
- `test_composition_failure_safe`: all sources raise, result=AVAILABLE.

## Files NOT to touch

- UI templates — PR-256 adds the chip.
- Cancellation policy CONSUMER logic — PR-257 builds on this; out of scope here.

## Implementation notes

Failure-safe default of AVAILABLE is deliberate. If observability subsystem is broken, daemon should default to "operator might be looking" (continue work) rather than "operator definitely not looking" (pause). Wrong default risks API spend; right default risks operator confusion. The policy errs toward continuing work; pause requires positive AWAY signal.

Heartbeat TTL 5 minutes: balance between "operator still here, just reading" (longer TTL good) and "operator left without saying" (shorter TTL good). 5 min works in v1; tunable later.

ActiveHoursSource default 9-21 Europe/Rome matches operator's location. Configurable per repo would be over-engineering for v1; single-operator system needs single window.

After this PR ships, the substrate exists for PR-256 (chip UI) and PR-257 (blocked_set consumer). Manual override is also live: operator can set Redis key directly via `redis-cli set operator_override AWAY` for testing before chip UI lands.
