# PR-108a: 100% coverage for fix._monitor_fix_idle

Branch: pr-108a-coverage-fix-monitor
- Type: refactor
- Complexity: medium
- Depends on: none
- Priority: 3
- Coder: any

## Problem

First of two sub-PRs for `src/daemon/handlers/fix.py` (67% → 100%). Covers `_monitor_fix_idle()` helper at lines 20-74.

Missing lines in scope: `36`, `45-46`, `51-73`. Total ~26 missing lines.

## Scope

Bring `_monitor_fix_idle()` to covered. Do NOT touch `handle_fix` (PR-108b owns).

Add tests in `tests/test_runner.py` per project convention.

## Missing blocks

- Line 36: branch within the monitor helper, likely a condition on state
- Lines 45-46: paired branch
- Lines 51-73: 23-line LARGE block — probably the main monitoring loop (idle detection since last push)

`_monitor_fix_idle` monitors the FIX cycle for idle-timeout condition per PR-045. When the coder has been quiet for `fix_idle_timeout_sec`, this helper triggers a nudge or escalation.

## Triage

All blocks likely test-addable. The 51-73 block is the core monitoring logic which is real operational path.

## Testing approach

The monitor is async. Mock:
- `datetime.now` / use frozen time for timeout calculation
- `git_ops._git` for push-timestamp retrieval
- `asyncio.sleep` patched to return immediately

Test scenarios:
- Recent push, not idle → continue monitoring
- Old push past threshold → trigger escalation
- No push at all → fall-through behavior
- Git errors during timestamp check → handle gracefully

## Tests to add in `tests/test_runner.py`

Indicative:
- `test_monitor_fix_idle_continues_when_recent_push`
- `test_monitor_fix_idle_escalates_when_idle_exceeds_threshold`
- `test_monitor_fix_idle_handles_missing_push_history`
- `test_monitor_fix_idle_handles_git_timeout_gracefully`
- 1-2 additional tests for specific branches in 51-73

## Success criteria

- `pytest tests/test_runner.py --cov=src/daemon/handlers/fix --cov-report=term-missing` runs with exit 0
- Lines 36, 45-46, 51-73 no longer appear in "Missing" column for fix.py
- Lines 135-308 MAY still appear (scope of PR-108b)
- All existing tests pass
- `ruff check src tests` exits 0
- `scripts/ci.sh` exits 0
- No diff in `src/daemon/handlers/fix.py` except justified deletions/pragmas

## Restrictions

- Do NOT touch lines 75+ in fix.py (handle_fix is PR-108b)
- Do NOT modify any other source module
- Do NOT change fix_idle_timeout semantics
- Do NOT add new features
