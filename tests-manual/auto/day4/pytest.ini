[pytest]
testpaths = tests-manual/auto/day4/tests
python_files = test_*.py
python_functions = test_*

# Playwright fixtures
addopts = 
    --browser=chromium
    -v
    --tb=short

# Custom markers
markers =
    order: run tests in specific order

# Timeout per test (external scenarios can be long)
